# -- Master switch for the whole chart. When `false`, every template renders nothing - use this
# to keep a release/namespace registered with a deployment system (e.g. an ArgoCD Application
# that always gets generated for every app/env combination) without actually deploying any
# resource into it. Note this does NOT cover subchart dependencies added via `Chart.yaml` (e.g. a
# bundled database/cache) - those still need their own `enabled: false` alongside this one.
# @section -- General
enabled: true
# -- Provide a name in place of the default application name.
# @section -- General
nameOverride: ""
# -- String to fully override the default application name.
# @section -- General
fullnameOverride: ""
# -- Add labels to all the deployed resources
# @section -- General
commonLabels: {}
global:
  # -- List or map of `configMapRef`/`secretRef` entries to load into every container's
  # `envFrom` (merged with each component's own `envFrom`, global entries first).
  # @section -- Global
  envFrom: []
  # - configMapRef:
  #     name: "my-config"
  # - secretRef:
  #     name: "my-secret"
  #
  # my-config:
  #   configMapRef:
  #     name: "my-config"
  # my-secret:
  #   secretRef:
  #     name: "my-secret"
  # -- Map or array of environment variables to inject into all containers (`valueFrom` supported).
  # @section -- Global
  env: {}
  # A: "A"
  # B:
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # C:
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  #
  # - name: "A"
  #   value: "A"
  # - name: "B"
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # - name: "C"
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  # -- Map of environment variables to inject into a configmap loaded by all containers (`valueFrom` not supported).
  # @section -- Global
  envCm: {}
  # D: "D"
  # -- Map of environment variables to inject into a secret loaded by all containers (`valueFrom` not supported).
  # @section -- Global
  envSecret: {}
  # E: "E"

  # -- Global Docker image registry
  # @section -- Global
  imageRegistry: ""
  # -- Image credentials applied to every component in addition to any component-specific `imagePullSecrets`.
  # @section -- Global
  imagePullSecrets: []
  # - name: "pullsecret-name-1"
  #   create: false
  # - name: "pullsecret-name-2"
  #   create: true
  #   registry: ""
  #   username: ""
  #   password: ""
  #   email: ""
  ## Chart-level Ingress, for fanning several components (or several charts) behind one hostname.
  ## Unlike `frontend.ingress` it has no default backend, so every path must name the service
  ## it routes to - rendering would otherwise point at a Service that does not exist.
  ingress:
    # -- Whether or not the chart-level ingress should be enabled.
    # @section -- Global
    enabled: false
    # -- Defines which ingress controller will implement the resource.
    # @section -- Global
    className: ""
    # -- Additional ingress annotations.
    # @section -- Global
    annotations: {}
    # -- Additional ingress labels.
    # @section -- Global
    labels: {}
    # -- Hosts and paths served by the chart-level ingress. Each path's `backend.serviceName` is
    # required (see above); `backend.portNumber` defaults to 80.
    # @section -- Global
    hosts: []
    # - name: "domain.local"
    #   paths:
    #   - path: "/"
    #     pathType: "Prefix"
    #     backend:
    #       serviceName: "my-web-service"
    #       portNumber: 80
    #   - path: "/api"
    #     pathType: "Prefix"
    #     backend:
    #       serviceName: "my-api-service"
    #       portNumber: 8080
    # -- TLS configuration for the chart-level ingress.
    # @section -- Global
    tls: []
    # - secretName: domain.local-tls
    #   hosts:
    #   - domain.local
  ## Chart-level Gateway API HTTPRoute, same rationale as `global.ingress` above: `rules` and their
  ## `backendRefs[].name` are required because there is no default backend to fall back to.
  httpRoute:
    # -- Whether or not the chart-level HTTPRoute should be enabled.
    # @section -- Global
    enabled: false
    # -- Additional HTTPRoute annotations.
    # @section -- Global
    annotations: {}
    # -- Additional HTTPRoute labels.
    # @section -- Global
    labels: {}
    # -- Parent references (Gateways) to attach the HTTPRoute to.
    # @section -- Global
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace
    #   sectionName: https
    # -- Hostnames for the HTTPRoute to match.
    # @section -- Global
    hostnames: []
    # - "example.com"
    # - "*.example.com"
    # -- Routing rules for the HTTPRoute. Required when `enabled` is true, and every `backendRefs`
    # entry must carry a `name`.
    # @section -- Global
    rules: []
    # - matches:
    #   - path:
    #       type: PathPrefix
    #       value: "/"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #     queryParams:
    #     - type: Exact
    #       name: "debug"
    #       value: "true"
    #     method: GET
    #   filters:
    #   - type: RequestHeaderModifier
    #     requestHeaderModifier:
    #       add:
    #       - name: "x-custom-header"
    #         value: "my-value"
    #   backendRefs:
    #   - name: my-service
    #     port: 80
    #     weight: 100
## Gateway API configuration (alternative to Ingress)
## Ref: https://gateway-api.sigs.k8s.io/
gateway:
  # -- Create a Gateway resource. Usually, you reference an existing Gateway managed by the infrastructure team.
  # @section -- Global
  create: false
  # -- Name of the Gateway resource. If not set, uses the release fullname.
  # @section -- Global
  name: ""
  # -- GatewayClass name. Required when creating a Gateway.
  # @section -- Global
  className: ""
  # -- Additional gateway annotations.
  # @section -- Global
  annotations: {}
  # -- Additional gateway labels.
  # @section -- Global
  labels: {}
  # -- Gateway addresses configuration.
  # @section -- Global
  addresses: []
  # - type: IPAddress
  #   value: "192.168.1.100"
  # - type: Hostname
  #   value: "gateway.example.com"
  # -- Gateway listeners configuration.
  # @section -- Global
  listeners: []
  # - name: http
  #   port: 80
  #   protocol: HTTP
  #   hostname: "*.example.com"
  #   allowedRoutes:
  #     namespaces:
  #       from: Same  # Same, All, or Selector
  #     kinds:
  #     - kind: HTTPRoute
  # - name: https
  #   port: 443
  #   protocol: HTTPS
  #   hostname: "*.example.com"
  #   tls:
  #     mode: Terminate  # Terminate or Passthrough
  #     certificateRefs:
  #     - kind: Secret
  #       name: example-com-tls
  #   allowedRoutes:
  #     namespaces:
  #       from: Same
frontend:
  # -- Workload kind to deploy the app as. One of "Deployment", "StatefulSet" or "DaemonSet"
  # (validated at render time - an unknown value fails instead of producing a release with no
  # workload). Use the top-level `jobs` / `cronjobs` maps for one-off or scheduled workloads.
  # Some values only apply to certain kinds: `replicaCount`/`autoscaling` and `strategy` are
  # Deployment-only (`autoscaling` also works on a StatefulSet), `volumeClaims`/`extraVolumeClaims`
  # are StatefulSet-only, and `updateStrategy` covers StatefulSet and DaemonSet.
  # @section -- Frontend
  deploymentType: "Deployment"
  # -- The number of application controller pods to run. Ignored when `deploymentType` is
  # "DaemonSet" (one pod per node) or when `autoscaling.enabled` is true.
  # @section -- Frontend
  replicaCount: 1
  image:
    # -- Registry to use for the app.
    # @section -- Frontend
    registry: "ghcr.io"
    # -- Repository to use for the app.
    # @section -- Frontend
    repository: "ia-generative/dig-dig-doc/frontend"
    # -- Image pull policy for the app.
    # @section -- Frontend
    pullPolicy: "IfNotPresent"
    # -- Tag to use for the app.
    # @section -- Frontend
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
    # -- Image digest (`sha256:...`). When set it takes precedence over `tag`, pinning the exact
    # image content so the same release can never resolve to a different build - preferred over a
    # mutable tag for anything you deploy to production.
    # @section -- Frontend
    digest: ""
  ingress:
    # -- Whether or not ingress should be enabled.
    # @section -- Frontend
    enabled: false
    # -- Defines which ingress controller will implement the resource.
    # @section -- Frontend
    className: ""
    # -- Additional ingress annotations.
    # @section -- Frontend
    annotations: {}
    # -- Additional ingress labels.
    # @section -- Frontend
    labels: {}
    hosts:
      - # -- Name of the host record.
        # @section -- Frontend
        name: "domain.local"
        # -- Paths of the host record to manage routing (avoids repeating the same host for
        # multiple paths/backends).
        # @section -- Frontend
        paths:
          - # -- Path of the host record to manage routing.
            # @section -- Frontend
            path: "/"
            # -- Path type of the host record.
            # @section -- Frontend
            pathType: "Prefix"
            backend:
              # -- Name of the backend service linked to the path (leave empty to use the app service).
              # @section -- Frontend
              serviceName: ""
              # -- Port used by the backend service linked to the path (leave null to use the app service port).
              # @section -- Frontend
              portNumber: null
    # -- Enable TLS configuration.
    # @section -- Frontend
    tls: []
    # - secretName: domain.local-tls
    #   hosts:
    #   - domain.local
  ## Gateway API HTTPRoute configuration (alternative to Ingress)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/httproute/
  httpRoute:
    # -- Enable an HTTPRoute resource for this service.
    # @section -- Frontend
    enabled: false
    # -- Additional HTTPRoute annotations.
    # @section -- Frontend
    annotations: {}
    # -- Additional HTTPRoute labels.
    # @section -- Frontend
    labels: {}
    # -- Parent references (Gateways) to attach the HTTPRoute to.
    # @section -- Frontend
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace  # optional
    #   sectionName: https            # optional, specific listener name
    # -- Hostnames for the HTTPRoute to match.
    # @section -- Frontend
    hostnames: []
    # - "example.com"
    # - "api.example.com"
    # -- Routing rules for the HTTPRoute.
    # @section -- Frontend
    rules: []
    # - matches:
    #   - path:
    #       type: PathPrefix     # PathPrefix, Exact, or RegularExpression
    #       value: "/"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #     method: GET
    #   filters:
    #   - type: RequestHeaderModifier
    #     requestHeaderModifier:
    #       add:
    #       - name: "x-custom-header"
    #         value: "my-value"
    #   backendRefs:
    #   - name: my-service       # defaults to chart service if not set
    #     port: 80               # defaults to service port if not set
    #     weight: 100            # for traffic splitting
  ## Gateway API GRPCRoute configuration (for gRPC services)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/grpcroute/
  grpcRoute:
    # -- Enable a GRPCRoute resource for this service.
    # @section -- Frontend
    enabled: false
    # -- Additional GRPCRoute annotations.
    # @section -- Frontend
    annotations: {}
    # -- Additional GRPCRoute labels.
    # @section -- Frontend
    labels: {}
    # -- Parent references (Gateways) to attach the GRPCRoute to.
    # @section -- Frontend
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace
    #   sectionName: grpc
    # -- Hostnames for the GRPCRoute to match.
    # @section -- Frontend
    hostnames: []
    # - "grpc.example.com"
    # -- Routing rules for the GRPCRoute.
    # @section -- Frontend
    rules: []
    # - matches:
    #   - method:
    #       type: Exact          # Exact or RegularExpression
    #       service: "mypackage.MyService"
    #       method: "MyMethod"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #   backendRefs:
    #   - name: my-grpc-service
    #     port: 50051
    #     weight: 100
  # -- Mount the ServiceAccount token into the app pods. Defaults to false so a compromised
  # container holds no API credentials; the API server does not need the token unless the app
  # actually talks to the Kubernetes API. Applied at pod level so it holds even when
  # `serviceAccount.name` points at an SA that automounts.
  # @section -- Frontend
  automountServiceAccountToken: false
  # -- Inject the legacy `{SVC}_SERVICE_HOST`/`_PORT` environment variables for every Service in the
  # namespace. Defaults to false: the variables are rarely used, leak the namespace's topology into
  # every container, and can collide with the app's own configuration. Set to true only for an app
  # that genuinely reads them.
  # @section -- Frontend
  enableServiceLinks: false
  # -- Annotations for the app deployed pods.
  # @section -- Frontend
  podAnnotations: {}
  # -- Labels for the app deployed pods.
  # @section -- Frontend
  podLabels: {}
  # -- Pod-level security context. Defaults to a hardened baseline that satisfies the `restricted`
  # Pod Security Standard. Rendered via `toYaml`, so any `PodSecurityContext` field is accepted.
  # Adjust the UID/GID to whatever your image actually ships with - `runAsNonRoot` makes the
  # kubelet refuse to start a container that would run as root, which is the intended failure mode
  # rather than something to switch off. Set to `null` to omit the block entirely.
  # @section -- Frontend
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    # Owns mounted volumes so a non-root container can still write to them.
    fsGroup: 1000
    # Only chown volume contents when the top-level dir doesn't already match, which avoids
    # re-chowning large volumes on every start.
    fsGroupChangePolicy: "OnRootMismatch"
    seccompProfile:
      type: "RuntimeDefault"
  # -- Init containers to add to the app pod.
  # @section -- Frontend
  initContainers: []
  # - name: wait-for-keycloak
  #   image: docker.io/curlimages/curl:latest
  #   command:
  #   - "/bin/sh"
  #   - "-c"
  #   args:
  #   - "while [ $(curl -sw '%{http_code}' http://webapp.svc.cluster.local -o /dev/null) -ne 200 ]; do sleep 5; echo 'Waiting for the webapp...'; done"
  #   volumeMounts:
  #   - mountPath: /custom-volume
  #     name: custom-volume
  # -- Extra containers to add to the app pod as sidecars.
  # @section -- Frontend
  extraContainers: []
  # - name: fluentd
  #   image: "fluentd"
  #   volumeMounts:
  #     - mountPath: /my-volume/config
  #       name: config
  # -- Frontend container port number. Set to `null`/`0` (and disable `service`/probes) for
  # components that don't listen on any port (e.g. a queue consumer).
  # @section -- Frontend
  containerPort: &frontendContainerPort 8080
  # -- Frontend container port name.
  # @section -- Frontend
  containerPortName: "http"
  # -- Frontend extra container ports.
  # @section -- Frontend
  extraPorts: []
  # - containerPort: 9090
  #   protocol: "TCP"
  # -- Frontend container command.
  # @section -- Frontend
  command: []
  # -- Frontend container command args.
  # @section -- Frontend
  args: []
  # -- Container-level security context. Defaults to a hardened baseline that satisfies the
  # `restricted` Pod Security Standard: no privilege escalation, no capabilities, immutable root
  # filesystem. Rendered via `toYaml`, so any `SecurityContext` field is accepted. Note
  # `readOnlyRootFilesystem` requires the app to write only to mounted volumes - the default
  # `volumes`/`volumeMounts` below provide an `emptyDir` on /tmp for that reason. Set to `null` to
  # omit the block entirely.
  # @section -- Frontend
  securityContext:
    allowPrivilegeEscalation: false
    privileged: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    capabilities:
      drop:
        - "ALL"
  # -- Frontend container env variables loaded from configmap or secret reference. List or map
  # (merged with `global.envFrom` above, global entries first); see `global.envFrom` for both forms.
  # @section -- Frontend
  envFrom: []
  # - configMapRef:
  #     name: my-config
  # - secretRef:
  #     name: my-secret
  # -- Map or array of environment variables to inject into the app container (`valueFrom` supported).
  # @section -- Frontend
  env: {}
  # A: "A"
  # B:
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # C:
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  #
  # - name: "A"
  #   value: "A"
  # - name: "B"
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # - name: "C"
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  # -- Map of environment variables to inject into a configmap loaded by the app container (`valueFrom` not supported).
  # @section -- Frontend
  envCm: {}
  # D: "D"
  # -- Map of environment variables to inject into a secret loaded by the app container (`valueFrom`
  # not supported). Values placed here are stored in plain text in the values file AND in the Helm
  # release secret, so use it for non-sensitive-but-secret-shaped config only. For real credentials
  # prefer referencing a Secret you manage elsewhere via `envFrom`, or have an operator materialise
  # it (see the `VaultStaticSecret` example under `extraObjects`).
  # @section -- Frontend
  envSecret: {}
  # E: "E"
  ## Each probe is rendered verbatim via `toYaml`, so any `Probe` field (`exec`, `tcpSocket`,
  ## `grpc`...) is accepted, not just the `httpGet` shown here. To DISABLE a probe, set it to
  ## `null` - `{}` does not work, because Helm deep-merges values files and would merge these
  ## defaults straight back in.
  probes:
    startupProbe:
      httpGet:
        # -- Frontend container healthcheck endpoint (startupProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Frontend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Frontend
        port: *frontendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Frontend
      initialDelaySeconds: 0
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Frontend
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Frontend
      failureThreshold: 10
      # -- How often (in seconds) to perform the probe.
      # @section -- Frontend
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Frontend
      timeoutSeconds: 5
    readinessProbe:
      httpGet:
        # -- Frontend container healthcheck endpoint (readinessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Frontend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Frontend
        port: *frontendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Frontend
      initialDelaySeconds: 10
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Frontend
      successThreshold: 2
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Frontend
      failureThreshold: 2
      # -- How often (in seconds) to perform the probe.
      # @section -- Frontend
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Frontend
      timeoutSeconds: 5
    livenessProbe:
      httpGet:
        # -- Frontend container healthcheck endpoint (livenessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Frontend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Frontend
        port: *frontendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Frontend
      initialDelaySeconds: 30
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Frontend
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Frontend
      failureThreshold: 3
      # -- How often (in seconds) to perform the probe.
      # @section -- Frontend
      periodSeconds: 30
      # -- Number of seconds after which the probe times out.
      # @section -- Frontend
      timeoutSeconds: 5
  ## Deployment-only. StatefulSet and DaemonSet use `updateStrategy` below - their update
  ## strategies are a different API shape (no `Recreate`, and different `rollingUpdate` fields),
  ## so they are kept as separate values rather than one block that means three different things.
  strategy:
    # -- Strategy type used to replace old Pods by new ones, can be `Recreate` or `RollingUpdate`.
    # Only applied when `deploymentType` is "Deployment".
    # @section -- Frontend
    type: "RollingUpdate"
    rollingUpdate:
      # -- The maximum number of pods that can be unavailable during the update process.
      # @section -- Frontend
      maxUnavailable: 1
      # -- The maximum number of pods that can be scheduled above the desired number of pods.
      # @section -- Frontend
      maxSurge: 1
  # -- Update strategy applied when `deploymentType` is "StatefulSet" or "DaemonSet" (ignored for
  # a Deployment, which uses `strategy` above). Rendered verbatim via `toYaml`, so it takes the
  # native `StatefulSetUpdateStrategy`/`DaemonSetUpdateStrategy` shape of the selected kind; left
  # empty, Kubernetes applies its own default (`RollingUpdate` for both).
  # @section -- Frontend
  updateStrategy: {}
  # StatefulSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     partition: 0
  #     maxUnavailable: 1
  # DaemonSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     maxUnavailable: 1
  #     maxSurge: 0
  # -- Image credentials configuration.
  # @section -- Frontend
  imagePullSecrets: []
  # - name: "pullsecret-name-1"
  #   create: false
  # - name: "pullsecret-name-2"
  #   create: true
  #   registry: ""
  #   username: ""
  #   password: ""
  #   email: ""
  ## Node-level pod settings. Mostly relevant when `deploymentType` is "DaemonSet" (node agents,
  ## log/metric collectors), but valid for every kind.
  # -- Share the host network namespace. Container ports then bind directly on the node, so they
  # must not collide with anything else running there.
  # @section -- Frontend
  hostNetwork: false
  # -- Share the host PID namespace (lets the container see and signal host processes).
  # @section -- Frontend
  hostPID: false
  # -- Pod DNS policy. Left empty, it defaults to `ClusterFirstWithHostNet` when `hostNetwork` is
  # true (otherwise a hostNetwork pod silently stops resolving cluster DNS) and to the Kubernetes
  # default `ClusterFirst` when it isn't.
  # @section -- Frontend
  # @default -- `""` (`ClusterFirstWithHostNet` when `hostNetwork` is true)
  dnsPolicy: ""
  # -- Pod DNS configuration, merged with `dnsPolicy` by the kubelet.
  # @section -- Frontend
  dnsConfig: {}
  # nameservers:
  # - "1.1.1.1"
  # options:
  # - name: "ndots"
  #   value: "2"
  # searches:
  # - "my.dns.search.suffix"
  # -- PriorityClass to schedule the pods with (e.g. `system-node-critical` for a node agent that
  # must not be evicted under pressure).
  # @section -- Frontend
  priorityClassName: ""
  # -- (int) Grace period, in seconds, given to the pod to shut down cleanly before it is killed.
  # @section -- Frontend
  # @default -- `null` (Kubernetes default of 30)
  terminationGracePeriodSeconds: null
  # -- Topology spread constraints used to spread the pods across failure domains.
  # @section -- Frontend
  topologySpreadConstraints: []
  # - maxSkew: 1
  #   topologyKey: "topology.kubernetes.io/zone"
  #   whenUnsatisfiable: "ScheduleAnyway"
  #   labelSelector:
  #     matchLabels:
  #       app.kubernetes.io/name: "digdigdoc-frontend"
  # -- Host aliases that will be injected at pod-level into /etc/hosts.
  # @section -- Frontend
  hostAliases: []
  # - ip: "127.0.0.1"
  #   hostnames:
  #   - "foo.local"
  #   - "bar.local"
  # - ip: "10.1.2.3"
  #   hostnames:
  #   - "foo.remote"
  #   - "bar.remote"
  # -- List of volumes to add. Prefer this for volumes the chart itself always needs (e.g.
  # security-hardening `emptyDir`s); use `extraVolumes` below for anything you add on top, so
  # overriding one doesn't require repeating the other. Defaults to a `/tmp` `emptyDir`, which is
  # what makes the default `securityContext.readOnlyRootFilesystem: true` usable - drop it only if
  # you also relax that. Helm replaces lists wholesale rather than merging them, so overriding this
  # key means restating the entries you want to keep.
  # @section -- Frontend
  volumes:
    - name: "tmp"
      emptyDir: {}
  # - name: config-volume
  #   configMap:
  #     name: log-config
  #     items:
  #       - key: log_level
  #         path: log_level.conf
  # - name: cache-volume
  #   emptyDir:
  #     sizeLimit: 500Mi
  # - name: image-volume
  #   image:
  #     reference: quay.io/crio/artifact:v2
  #     pullPolicy: IfNotPresent
  # -- Additional volumes to add, concatenated with `volumes` above at render time (e.g. to
  # mount a cert or config from a values override without repeating the chart's own volumes).
  # @section -- Frontend
  extraVolumes: []
  # - name: certs
  #   secret:
  #     secretName: my-tls-cert
  # -- List of volumeClaims to add, rendered as the StatefulSet's `volumeClaimTemplates`. Requires
  # `deploymentType: "StatefulSet"` - setting it on a Deployment or DaemonSet fails at render time
  # rather than being silently dropped (use `volumes`/`extraVolumes` there instead).
  # @section -- Frontend
  volumeClaims: []
  # - metadata:
  #     name: app-volume
  #   spec:
  #     accessModes: [ "ReadWriteOnce" ]
  #     storageClassName: "my-storage-class"
  #     resources:
  #       requests:
  #         storage: 1Gi
  # -- Additional volumeClaims to add, concatenated with `volumeClaims` above at render time.
  # @section -- Frontend
  extraVolumeClaims: []
  # -- List of mounts to add (normally used with `volumes` or `volumeClaims`). Prefer this for
  # mounts the chart itself always needs; use `extraVolumeMounts` below for anything you add
  # on top, so overriding one doesn't require repeating the other. Defaults to the `/tmp` mount
  # backing the hardened `readOnlyRootFilesystem` default (see `volumes` above).
  # @section -- Frontend
  volumeMounts:
    - name: "tmp"
      mountPath: "/tmp"
  # - name: config-volume
  #   mountPath: /app/config
  #   subPath: config
  #   readOnly: true
  #   mountPropagation: None
  #   recursiveReadOnly: Enabled
  # - name: cache-volume
  #   mountPath: /app/cache
  # - name: image-volume
  #   mountPath: /app/content
  # - name: storage-volume
  #   mountPath: /app/storage
  # -- Additional volumeMounts to add, concatenated with `volumeMounts` above at render time.
  # @section -- Frontend
  extraVolumeMounts: []
  # - name: certs
  #   mountPath: /etc/certs
  #   readOnly: true
  service:
    # -- Whether or not to create a Service for the app. Set to `false` for components that
    # don't accept traffic (e.g. a queue consumer with no `containerPort`).
    # @section -- Frontend
    enabled: true
    # -- Type of service to create for the app.
    # @section -- Frontend
    type: "ClusterIP"
    # -- Port used by the service.
    # @section -- Frontend
    port: 80
    # -- (int) Port used when type is `NodePort` to expose the service on the given node port. Left
    # empty, Kubernetes allocates one from the configured node-port range, which avoids two
    # releases of this chart colliding on the same hardcoded port.
    # @section -- Frontend
    # @default -- `null` (allocated by Kubernetes)
    nodePort: null
    # -- Port name used by the service.
    # @section -- Frontend
    portName: "http"
    # -- Protocol used by the service.
    # @section -- Frontend
    protocol: "TCP"
    # -- Extra service ports.
    # @section -- Frontend
    extraPorts: []
    # - port: 9090
    #   targetPort: 9090
    #   protocol: "TCP"
    #   name: "metrics"
  resources:
    requests:
      # -- Memory request for the app.
      # @section -- Frontend
      memory: "256Mi"
      # -- CPU request for the app.
      # @section -- Frontend
      cpu: "100m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "100Mi"
    limits:
      # -- Memory limit for the app.
      # @section -- Frontend
      memory: "2Gi"
      # -- CPU limit for the app.
      # @section -- Frontend
      cpu: "500m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "500Mi"
  autoscaling:
    # -- Enable Horizontal Pod Autoscaler for the app.
    # @section -- Frontend
    enabled: false
    # -- Minimum number of replicas for the app.
    # @section -- Frontend
    minReplicas: 1
    # -- Maximum number of replicas for the app.
    # @section -- Frontend
    maxReplicas: 3
    # -- Average CPU utilization percentage for the app.
    # @section -- Frontend
    targetCPUUtilizationPercentage: 80
    # -- Average memory utilization percentage for the app.
    # @section -- Frontend
    targetMemoryUtilizationPercentage: 80
  # -- Revision history limit for the app.
  # @section -- Frontend
  revisionHistoryLimit: 10
  # -- Default node selector for app.
  # @section -- Frontend
  nodeSelector: {}
  # kubernetes.io/os: "linux"
  # kubernetes.io/arch: "amd64"
  # kubernetes.io/hostname: "node1"
  # -- Default tolerations for app.
  # @section -- Frontend
  tolerations: []
  # - key: "key1"
  #   operator: "Equal"
  #   value: "value1"
  #   effect: "NoSchedule"
  #   tolerationSeconds: 3600
  # - key: "key2"
  #   operator: "Equal"
  #   value: "value2"
  #   effect: "NoExecute"
  #   tolerationSeconds: 3600
  # - key: "key3"
  #   operator: "Exists"
  #   effect: "PreferNoSchedule"
  #   tolerationSeconds: 3600
  # -- Affinity used for app pod.
  # @section -- Frontend
  affinity: {}
  # podAntiAffinity:
  #   requiredDuringSchedulingIgnoredDuringExecution:
  #   - labelSelector:
  #       matchExpressions:
  #       - key: "app.kubernetes.io/name"
  #         operator: "In"
  #         values:
  #         - "digdigdoc-frontend"
  #     topologyKey: "kubernetes.io/hostname"
  serviceAccount:
    # -- Enable the service account.
    # @section -- Frontend
    enabled: false
    # -- Create a service account.
    # @section -- Frontend
    create: false
    # -- Annotations applied to created service account.
    # @section -- Frontend
    annotations: {}
    # -- Service account name.
    # @section -- Frontend
    name: ""
    # -- Should the service account access token be automount in the pod.
    # @section -- Frontend
    automountServiceAccountToken: false
    role:
      # -- Should the role be created.
      # @section -- Frontend
      create: false
      # -- Role rules associated with the service account.
      # @section -- Frontend
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
    clusterRole:
      # -- Should the clusterRole be created.
      # @section -- Frontend
      create: false
      # -- ClusterRole rules associated with the service account.
      # @section -- Frontend
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
  ## Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Deploy a PodDisruptionBudget for the app
    # @section -- Frontend
    enabled: false
    # -- Labels to be added to app pdb.
    # @section -- Frontend
    labels: {}
    # -- Annotations to be added to app pdb.
    # @section -- Frontend
    annotations: {}
    # -- Number of pods that are available after eviction as number or percentage (eg.: 50%). One
    # of `minAvailable` / `maxUnavailable` must be set when `pdb.enabled` is true - a budget of 0
    # is the same as having no budget at all, so leaving both empty fails at render time.
    # @section -- Frontend
    minAvailable: ""
    # -- Number of pods that are unavailable after eviction as number or percentage (eg.: 50%). Has higher precedence over `frontend.pdb.minAvailable`.
    # @section -- Frontend
    maxUnavailable: ""
  networkPolicy:
    # -- Create NetworkPolicy object for the app. The policy always selects this component's pods
    # only (via its selector labels), never the whole namespace.
    # @section -- Frontend
    create: false
    # -- Labels to be added to the app NetworkPolicy.
    # @section -- Frontend
    labels: {}
    # -- Annotations to be added to the app NetworkPolicy.
    # @section -- Frontend
    annotations: {}
    # -- Policy types used in the NetworkPolicy object.
    # @section -- Frontend
    policyTypes:
      - Ingress
    # -- Ingress rules for the NetworkPolicy object.
    # @section -- Frontend
    ingress: []
    # - from:
    #   - ipBlock:
    #       cidr: 172.17.0.0/16
    #       except:
    #       - 172.17.1.0/24
    #   - namespaceSelector:
    #       matchLabels:
    #         project: myproject
    #   - podSelector:
    #       matchLabels:
    #         role: frontend
    #   ports:
    #   - protocol: TCP
    #     port: 6379
    # -- Egress rules for the NetworkPolicy object.
    # @section -- Frontend
    egress: []
    # - to:
    #   - ipBlock:
    #       cidr: 10.0.0.0/24
    #   ports:
    #   - protocol: TCP
    #     port: 32000
    #     endPort: 32768
  metrics:
    # -- Deploy metrics service.
    # @section -- Frontend
    enabled: false
    service:
      # -- Type of metrics service to create.
      # @section -- Frontend
      type: "ClusterIP"
      # -- Metrics service annotations.
      # @section -- Frontend
      annotations: {}
      # -- Metrics service labels.
      # @section -- Frontend
      labels: {}
      # -- Metrics service port name.
      # @section -- Frontend
      portName: "metrics"
      # -- Metrics service port.
      # @section -- Frontend
      port: 9000
      # -- Metrics service target port.
      # @section -- Frontend
      targetPort: 9000
    serviceMonitor:
      # -- Enable a prometheus ServiceMonitor.
      # @section -- Frontend
      enabled: false
      # -- Prometheus ServiceMonitor labels.
      # @section -- Frontend
      labels: {}
      # -- Prometheus ServiceMonitor annotations.
      # @section -- Frontend
      annotations: {}
      endpoints:
        - basicAuth:
            # -- The secret in the service monitor namespace that contains the username for authentication.
            # @section -- Frontend
            username: ""
            # -- The secret in the service monitor namespace that contains the password for authentication.
            # @section -- Frontend
            password: ""
          bearerTokenSecret:
            # -- Secret name to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Frontend
            name: ""
            # -- Secret key to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Frontend
            key: ""
          # -- Prometheus ServiceMonitor interval.
          # @section -- Frontend
          interval: "30s"
          # -- Prometheus ServiceMonitor scrapeTimeout. If empty, Prometheus uses the global scrape timeout unless it is less than the target's scrape interval value in which the latter is used.
          # @section -- Frontend
          scrapeTimeout: "10s"
          # -- Path used by the Prometheus ServiceMonitor to scrape metrics.
          # @section -- Frontend
          path: "/metrics"
          # -- When true, honorLabels preserves the metric’s labels when they collide with the target’s labels.
          # @section -- Frontend
          honorLabels: false
          # -- Prometheus RelabelConfigs to apply to samples before scraping.
          # @section -- Frontend
          relabelings: []
          # -- Prometheus MetricRelabelConfigs to apply to samples before ingestion.
          # @section -- Frontend
          metricRelabelings: []
          # -- Prometheus ServiceMonitor selector.
          # @section -- Frontend
          selector: {}
          # prometheus: kube-prometheus
          # -- Prometheus ServiceMonitor scheme.
          # @section -- Frontend
          scheme: ""
          # -- Prometheus ServiceMonitor tlsConfig.
          # @section -- Frontend
          tlsConfig: {}
# -- Map of Jobs to create (e.g. one-off DB migrations, data seeding, archiving...). Each key
# is used as the job name and as its `app.kubernetes.io/component` label. Every entry accepts
# the fields documented in the commented example below.
# @section -- General
jobs:
  migration:
    # -- Run as a Helm hook so migrations execute before the new version
    # serves traffic (pre-install + pre-upgrade).
    hook:
      enabled: true
      types:
        - "pre-install"
        - "pre-upgrade"
      weight: 0
      deletePolicy: "before-hook-creation,hook-succeeded"
    image:
      registry: "ghcr.io"
      repository: "ia-generative/dig-dig-doc/backend"
      pullPolicy: "IfNotPresent"
      tag: ""
      digest: ""
    imagePullSecrets: []
    serviceAccount:
      enabled: false
      create: false
      name: ""
      annotations: {}
      automountServiceAccountToken: false
    podAnnotations: {}
    podLabels: {}
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
      fsGroupChangePolicy: "OnRootMismatch"
      seccompProfile:
        type: "RuntimeDefault"
    securityContext:
      allowPrivilegeEscalation: false
      privileged: false
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
      capabilities:
        drop: ["ALL"]
    initContainers: []
    extraContainers: []
    command: []
    args:
      - "alembic"
      - "upgrade"
      - "head"
    envFrom: []
    env:
      DATABASE_URL:
        value: "postgresql+asyncpg://digdigdoc:digdigdoc@digdigdoc-postgres:5432/digdigdoc"
    envCm: {}
    envSecret: {}
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    volumes:
      - name: "tmp"
        emptyDir: {}
    extraVolumes: []
    volumeMounts:
      - name: "tmp"
        mountPath: "/tmp"
    extraVolumeMounts: []
    hostAliases: []
    nodeSelector: {}
    affinity: {}
    tolerations: []
    backoffLimit: 3
    ttlSecondsAfterFinished: 300
    activeDeadlineSeconds: null
    completions: null
    parallelism: 1
    restartPolicy: "Never"
# migration:
#   # -- Run this job as a Helm hook instead of a normal templated resource, so it executes
#   # at a specific point in the release lifecycle (e.g. before serving traffic with the new
#   # version). Leave disabled to have the job run like any other resource on every apply.
#   hook:
#     enabled: true
#     types:
#     - "post-install"
#     - "post-upgrade"
#     weight: 0
#     deletePolicy: "before-hook-creation,hook-succeeded"
#   image:
#     registry: "docker.io"
#     repository: "debian"
#     pullPolicy: "IfNotPresent"
#     tag: "9.9.9"
#     # Takes precedence over `tag` when set - prefer it for anything production-facing.
#     digest: ""
#   imagePullSecrets: []
#   # - name: "pullsecret-name-1"
#   #   create: false
#   serviceAccount:
#     enabled: false
#     create: false
#     name: ""
#     annotations: {}
#     automountServiceAccountToken: false
#   podAnnotations: {}
#   podLabels: {}
#   # Job pods get no implicit security context - unlike the `frontend` component there are no
#   # chart defaults to inherit, so declare the same hardened baseline on every entry you add.
#   podSecurityContext:
#     runAsNonRoot: true
#     runAsUser: 1000
#     runAsGroup: 1000
#     fsGroup: 1000
#     fsGroupChangePolicy: "OnRootMismatch"
#     seccompProfile:
#       type: "RuntimeDefault"
#   securityContext:
#     allowPrivilegeEscalation: false
#     privileged: false
#     readOnlyRootFilesystem: true
#     runAsNonRoot: true
#     runAsUser: 1000
#     runAsGroup: 1000
#     capabilities:
#       drop: ["ALL"]
#   initContainers: []
#   extraContainers: []
#   command: []
#   args: []
#   # - "migrate"
#   # - "--yes"
#   envFrom: []
#   env: {}
#   envCm: {}
#   envSecret: {}
#   resources: {}
#   volumes: []
#   extraVolumes: []
#   volumeMounts: []
#   extraVolumeMounts: []
#   hostAliases: []
#   nodeSelector: {}
#   affinity: {}
#   tolerations: []
#   # -- Number of retries before the Job is considered failed.
#   backoffLimit: 3
#   # -- Number of seconds after which a finished Job (and its pods) are automatically cleaned up.
#   ttlSecondsAfterFinished: 300
#   # -- Number of seconds the pod can run before it is actively terminated.
#   activeDeadlineSeconds: null
#   # -- Desired number of successfully finished pods (leave unset for the default of 1).
#   completions: null
#   # -- Maximum number of pods the Job should run concurrently.
#   parallelism: 1
#   # -- Restart policy for the pod, must be "Never" or "OnFailure".
#   restartPolicy: "Never"

# -- Map of CronJobs to create (e.g. periodic archiving, cleanup, reports...). Each key is
# used as the cronjob name and as its `app.kubernetes.io/component` label. Every entry accepts
# the same fields as a `jobs` entry (see above, minus `hook`) plus the scheduling fields
# documented in the commented example below.
# @section -- General
cronjobs: {}
# archive:
#   # -- Cron expression defining when the job should run (required).
#   schedule: "0 2 * * *"
#   # -- Timezone the schedule is evaluated in (defaults to the kube-controller-manager's).
#   timeZone: ""
#   # -- How to treat concurrent executions, one of "Allow", "Forbid" or "Replace".
#   concurrencyPolicy: "Forbid"
#   # -- Pause new scheduled executions without deleting the CronJob.
#   suspend: false
#   # -- Deadline (in seconds) for starting the job if it misses its scheduled time.
#   startingDeadlineSeconds: 300
#   # -- Number of successful finished jobs to keep.
#   successfulJobsHistoryLimit: 3
#   # -- Number of failed finished jobs to keep.
#   failedJobsHistoryLimit: 1
#   image:
#     registry: "docker.io"
#     repository: "debian"
#     pullPolicy: "IfNotPresent"
#     tag: "9.9.9"
#   command: []
#   args: []
#   # - "archive"
#   # - "--older-than=30d"
#   envFrom: []
#   env: {}
#   envCm: {}
#   envSecret: {}
#   resources: {}
#   volumes: []
#   volumeMounts: []
#   restartPolicy: "Never"

# -- Map of extra specs to dynamically add to this chart. Each key is a unique, arbitrary name
# for the object (only used so `-f` values files/overrides can add, override or remove a single
# entry by key instead of the whole list - lists don't merge across values files in Helm).
# @section -- General
extraObjects: {}
# my-configmap:
#   apiVersion: v1
#   kind: ConfigMap
#   metadata:
#     name: my-configmap
#   data:
#     key: test
# my-vault-secret:
#   apiVersion: secrets.hashicorp.com/v1beta1
#   kind: VaultStaticSecret
#   metadata:
#     name: foo
#     labels:
#       foo/bar: "baz"
#     annotations:
#       foo/bar: "baz"
#   spec:
#     mount: "my-kv"
#     vaultAuthRef: "my-vault-auth"
#     path: "dev/foo"
#     version: 21
#     type: "kv-v2"
#     refreshAfter: "30s"
#     hmacSecretData: true
#     rolloutRestartTargets:
#     - kind: Deployment
#       name: frontend
#     destination:
#       create: true
#       overwrite: true
#       labels:
#         foo/bar: "baz"
#       annotations:
#         foo/bar: "baz"
#       name: foo
#       type: "Opaque"
## Additional services comming from dependencies can be configured below.
## Ref: https://artifacthub.io/packages/helm/this-is-tobi-helm-charts/cnpg-cluster
# cnpg:
#   enabled: false
#   fullnameOverride: frontend-pg-cluster
#   imageName: ghcr.io/cloudnative-pg/postgresql:17.6
#   mode: primary
#   dbName: frontend
#   credentials:
#     username: frontend
#   pvcSize:
#     data: 3Gi
#     wal: 2Gi
#   resources:
#     requests:
#       cpu: 250m
#       memory: 512Mi
#     limits:
#       cpu: 500m
#       memory: 1Gi

backend:
  # -- Workload kind to deploy the app as. One of "Deployment", "StatefulSet" or "DaemonSet"
  # (validated at render time - an unknown value fails instead of producing a release with no
  # workload). Use the top-level `jobs` / `cronjobs` maps for one-off or scheduled workloads.
  # Some values only apply to certain kinds: `replicaCount`/`autoscaling` and `strategy` are
  # Deployment-only (`autoscaling` also works on a StatefulSet), `volumeClaims`/`extraVolumeClaims`
  # are StatefulSet-only, and `updateStrategy` covers StatefulSet and DaemonSet.
  # @section -- Backend
  deploymentType: "Deployment"
  # -- The number of application controller pods to run. Ignored when `deploymentType` is
  # "DaemonSet" (one pod per node) or when `autoscaling.enabled` is true.
  # @section -- Backend
  replicaCount: 1
  image:
    # -- Registry to use for the app.
    # @section -- Backend
    registry: "ghcr.io"
    # -- Repository to use for the app.
    # @section -- Backend
    repository: "ia-generative/dig-dig-doc/backend"
    # -- Image pull policy for the app.
    # @section -- Backend
    pullPolicy: "IfNotPresent"
    # -- Tag to use for the app.
    # @section -- Backend
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
    # -- Image digest (`sha256:...`). When set it takes precedence over `tag`, pinning the exact
    # image content so the same release can never resolve to a different build - preferred over a
    # mutable tag for anything you deploy to production.
    # @section -- Backend
    digest: ""
  ingress:
    # -- Whether or not ingress should be enabled.
    # @section -- Backend
    enabled: false
    # -- Defines which ingress controller will implement the resource.
    # @section -- Backend
    className: ""
    # -- Additional ingress annotations.
    # @section -- Backend
    annotations: {}
    # -- Additional ingress labels.
    # @section -- Backend
    labels: {}
    hosts:
      - # -- Name of the host record.
        # @section -- Backend
        name: "domain.local"
        # -- Paths of the host record to manage routing (avoids repeating the same host for
        # multiple paths/backends).
        # @section -- Backend
        paths:
          - # -- Path of the host record to manage routing.
            # @section -- Backend
            path: "/"
            # -- Path type of the host record.
            # @section -- Backend
            pathType: "Prefix"
            backend:
              # -- Name of the backend service linked to the path (leave empty to use the app service).
              # @section -- Backend
              serviceName: ""
              # -- Port used by the backend service linked to the path (leave null to use the app service port).
              # @section -- Backend
              portNumber: null
    # -- Enable TLS configuration.
    # @section -- Backend
    tls: []
    # - secretName: domain.local-tls
    #   hosts:
    #   - domain.local
  ## Gateway API HTTPRoute configuration (alternative to Ingress)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/httproute/
  httpRoute:
    # -- Enable an HTTPRoute resource for this service.
    # @section -- Backend
    enabled: false
    # -- Additional HTTPRoute annotations.
    # @section -- Backend
    annotations: {}
    # -- Additional HTTPRoute labels.
    # @section -- Backend
    labels: {}
    # -- Parent references (Gateways) to attach the HTTPRoute to.
    # @section -- Backend
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace  # optional
    #   sectionName: https            # optional, specific listener name
    # -- Hostnames for the HTTPRoute to match.
    # @section -- Backend
    hostnames: []
    # - "example.com"
    # - "api.example.com"
    # -- Routing rules for the HTTPRoute.
    # @section -- Backend
    rules: []
    # - matches:
    #   - path:
    #       type: PathPrefix     # PathPrefix, Exact, or RegularExpression
    #       value: "/"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #     method: GET
    #   filters:
    #   - type: RequestHeaderModifier
    #     requestHeaderModifier:
    #       add:
    #       - name: "x-custom-header"
    #         value: "my-value"
    #   backendRefs:
    #   - name: my-service       # defaults to chart service if not set
    #     port: 80               # defaults to service port if not set
    #     weight: 100            # for traffic splitting
  ## Gateway API GRPCRoute configuration (for gRPC services)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/grpcroute/
  grpcRoute:
    # -- Enable a GRPCRoute resource for this service.
    # @section -- Backend
    enabled: false
    # -- Additional GRPCRoute annotations.
    # @section -- Backend
    annotations: {}
    # -- Additional GRPCRoute labels.
    # @section -- Backend
    labels: {}
    # -- Parent references (Gateways) to attach the GRPCRoute to.
    # @section -- Backend
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace
    #   sectionName: grpc
    # -- Hostnames for the GRPCRoute to match.
    # @section -- Backend
    hostnames: []
    # - "grpc.example.com"
    # -- Routing rules for the GRPCRoute.
    # @section -- Backend
    rules: []
    # - matches:
    #   - method:
    #       type: Exact          # Exact or RegularExpression
    #       service: "mypackage.MyService"
    #       method: "MyMethod"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #   backendRefs:
    #   - name: my-grpc-service
    #     port: 50051
    #     weight: 100
  # -- Mount the ServiceAccount token into the app pods. Defaults to false so a compromised
  # container holds no API credentials; the API server does not need the token unless the app
  # actually talks to the Kubernetes API. Applied at pod level so it holds even when
  # `serviceAccount.name` points at an SA that automounts.
  # @section -- Backend
  automountServiceAccountToken: false
  # -- Inject the legacy `{SVC}_SERVICE_HOST`/`_PORT` environment variables for every Service in the
  # namespace. Defaults to false: the variables are rarely used, leak the namespace's topology into
  # every container, and can collide with the app's own configuration. Set to true only for an app
  # that genuinely reads them.
  # @section -- Backend
  enableServiceLinks: false
  # -- Annotations for the app deployed pods.
  # @section -- Backend
  podAnnotations: {}
  # -- Labels for the app deployed pods.
  # @section -- Backend
  podLabels: {}
  # -- Pod-level security context. Defaults to a hardened baseline that satisfies the `restricted`
  # Pod Security Standard. Rendered via `toYaml`, so any `PodSecurityContext` field is accepted.
  # Adjust the UID/GID to whatever your image actually ships with - `runAsNonRoot` makes the
  # kubelet refuse to start a container that would run as root, which is the intended failure mode
  # rather than something to switch off. Set to `null` to omit the block entirely.
  # @section -- Backend
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    # Owns mounted volumes so a non-root container can still write to them.
    fsGroup: 1000
    # Only chown volume contents when the top-level dir doesn't already match, which avoids
    # re-chowning large volumes on every start.
    fsGroupChangePolicy: "OnRootMismatch"
    seccompProfile:
      type: "RuntimeDefault"
  # -- Init containers to add to the app pod.
  # @section -- Backend
  initContainers: []
  # - name: wait-for-keycloak
  #   image: docker.io/curlimages/curl:latest
  #   command:
  #   - "/bin/sh"
  #   - "-c"
  #   args:
  #   - "while [ $(curl -sw '%{http_code}' http://webapp.svc.cluster.local -o /dev/null) -ne 200 ]; do sleep 5; echo 'Waiting for the webapp...'; done"
  #   volumeMounts:
  #   - mountPath: /custom-volume
  #     name: custom-volume
  # -- Extra containers to add to the app pod as sidecars.
  # @section -- Backend
  extraContainers: []
  # - name: fluentd
  #   image: "fluentd"
  #   volumeMounts:
  #     - mountPath: /my-volume/config
  #       name: config
  # -- Backend container port number. Set to `null`/`0` (and disable `service`/probes) for
  # components that don't listen on any port (e.g. a queue consumer).
  # @section -- Backend
  containerPort: &backendContainerPort 8080
  # -- Backend container port name.
  # @section -- Backend
  containerPortName: "http"
  # -- Backend extra container ports.
  # @section -- Backend
  extraPorts: []
  # - containerPort: 9090
  #   protocol: "TCP"
  # -- Backend container command.
  # @section -- Backend
  command: []
  # -- Backend container command args.
  # @section -- Backend
  args: []
  # -- Container-level security context. Defaults to a hardened baseline that satisfies the
  # `restricted` Pod Security Standard: no privilege escalation, no capabilities, immutable root
  # filesystem. Rendered via `toYaml`, so any `SecurityContext` field is accepted. Note
  # `readOnlyRootFilesystem` requires the app to write only to mounted volumes - the default
  # `volumes`/`volumeMounts` below provide an `emptyDir` on /tmp for that reason. Set to `null` to
  # omit the block entirely.
  # @section -- Backend
  securityContext:
    allowPrivilegeEscalation: false
    privileged: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    capabilities:
      drop:
        - "ALL"
  # -- Backend container env variables loaded from configmap or secret reference. List or map
  # (merged with `global.envFrom` above, global entries first); see `global.envFrom` for both forms.
  # @section -- Backend
  envFrom: []
  # - configMapRef:
  #     name: my-config
  # - secretRef:
  #     name: my-secret
  # -- Map or array of environment variables to inject into the app container (`valueFrom` supported).
  # @section -- Backend
  env: {}
  # A: "A"
  # B:
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # C:
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  #
  # - name: "A"
  #   value: "A"
  # - name: "B"
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # - name: "C"
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  # -- Map of environment variables to inject into a configmap loaded by the app container (`valueFrom` not supported).
  # @section -- Backend
  envCm: {}
  # D: "D"
  # -- Map of environment variables to inject into a secret loaded by the app container (`valueFrom`
  # not supported). Values placed here are stored in plain text in the values file AND in the Helm
  # release secret, so use it for non-sensitive-but-secret-shaped config only. For real credentials
  # prefer referencing a Secret you manage elsewhere via `envFrom`, or have an operator materialise
  # it (see the `VaultStaticSecret` example under `extraObjects`).
  # @section -- Backend
  envSecret: {}
  # E: "E"
  ## Each probe is rendered verbatim via `toYaml`, so any `Probe` field (`exec`, `tcpSocket`,
  ## `grpc`...) is accepted, not just the `httpGet` shown here. To DISABLE a probe, set it to
  ## `null` - `{}` does not work, because Helm deep-merges values files and would merge these
  ## defaults straight back in.
  probes:
    startupProbe:
      httpGet:
        # -- Backend container healthcheck endpoint (startupProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Backend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Backend
        port: *backendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Backend
      initialDelaySeconds: 0
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Backend
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Backend
      failureThreshold: 10
      # -- How often (in seconds) to perform the probe.
      # @section -- Backend
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Backend
      timeoutSeconds: 5
    readinessProbe:
      httpGet:
        # -- Backend container healthcheck endpoint (readinessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Backend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Backend
        port: *backendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Backend
      initialDelaySeconds: 10
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Backend
      successThreshold: 2
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Backend
      failureThreshold: 2
      # -- How often (in seconds) to perform the probe.
      # @section -- Backend
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Backend
      timeoutSeconds: 5
    livenessProbe:
      httpGet:
        # -- Backend container healthcheck endpoint (livenessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Backend
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Backend
        port: *backendContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Backend
      initialDelaySeconds: 30
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Backend
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Backend
      failureThreshold: 3
      # -- How often (in seconds) to perform the probe.
      # @section -- Backend
      periodSeconds: 30
      # -- Number of seconds after which the probe times out.
      # @section -- Backend
      timeoutSeconds: 5
  ## Deployment-only. StatefulSet and DaemonSet use `updateStrategy` below - their update
  ## strategies are a different API shape (no `Recreate`, and different `rollingUpdate` fields),
  ## so they are kept as separate values rather than one block that means three different things.
  strategy:
    # -- Strategy type used to replace old Pods by new ones, can be `Recreate` or `RollingUpdate`.
    # Only applied when `deploymentType` is "Deployment".
    # @section -- Backend
    type: "RollingUpdate"
    rollingUpdate:
      # -- The maximum number of pods that can be unavailable during the update process.
      # @section -- Backend
      maxUnavailable: 1
      # -- The maximum number of pods that can be scheduled above the desired number of pods.
      # @section -- Backend
      maxSurge: 1
  # -- Update strategy applied when `deploymentType` is "StatefulSet" or "DaemonSet" (ignored for
  # a Deployment, which uses `strategy` above). Rendered verbatim via `toYaml`, so it takes the
  # native `StatefulSetUpdateStrategy`/`DaemonSetUpdateStrategy` shape of the selected kind; left
  # empty, Kubernetes applies its own default (`RollingUpdate` for both).
  # @section -- Backend
  updateStrategy: {}
  # StatefulSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     partition: 0
  #     maxUnavailable: 1
  # DaemonSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     maxUnavailable: 1
  #     maxSurge: 0
  # -- Image credentials configuration.
  # @section -- Backend
  imagePullSecrets: []
  # - name: "pullsecret-name-1"
  #   create: false
  # - name: "pullsecret-name-2"
  #   create: true
  #   registry: ""
  #   username: ""
  #   password: ""
  #   email: ""
  ## Node-level pod settings. Mostly relevant when `deploymentType` is "DaemonSet" (node agents,
  ## log/metric collectors), but valid for every kind.
  # -- Share the host network namespace. Container ports then bind directly on the node, so they
  # must not collide with anything else running there.
  # @section -- Backend
  hostNetwork: false
  # -- Share the host PID namespace (lets the container see and signal host processes).
  # @section -- Backend
  hostPID: false
  # -- Pod DNS policy. Left empty, it defaults to `ClusterFirstWithHostNet` when `hostNetwork` is
  # true (otherwise a hostNetwork pod silently stops resolving cluster DNS) and to the Kubernetes
  # default `ClusterFirst` when it isn't.
  # @section -- Backend
  # @default -- `""` (`ClusterFirstWithHostNet` when `hostNetwork` is true)
  dnsPolicy: ""
  # -- Pod DNS configuration, merged with `dnsPolicy` by the kubelet.
  # @section -- Backend
  dnsConfig: {}
  # nameservers:
  # - "1.1.1.1"
  # options:
  # - name: "ndots"
  #   value: "2"
  # searches:
  # - "my.dns.search.suffix"
  # -- PriorityClass to schedule the pods with (e.g. `system-node-critical` for a node agent that
  # must not be evicted under pressure).
  # @section -- Backend
  priorityClassName: ""
  # -- (int) Grace period, in seconds, given to the pod to shut down cleanly before it is killed.
  # @section -- Backend
  # @default -- `null` (Kubernetes default of 30)
  terminationGracePeriodSeconds: null
  # -- Topology spread constraints used to spread the pods across failure domains.
  # @section -- Backend
  topologySpreadConstraints: []
  # - maxSkew: 1
  #   topologyKey: "topology.kubernetes.io/zone"
  #   whenUnsatisfiable: "ScheduleAnyway"
  #   labelSelector:
  #     matchLabels:
  #       app.kubernetes.io/name: "digdigdoc-backend"
  # -- Host aliases that will be injected at pod-level into /etc/hosts.
  # @section -- Backend
  hostAliases: []
  # - ip: "127.0.0.1"
  #   hostnames:
  #   - "foo.local"
  #   - "bar.local"
  # - ip: "10.1.2.3"
  #   hostnames:
  #   - "foo.remote"
  #   - "bar.remote"
  # -- List of volumes to add. Prefer this for volumes the chart itself always needs (e.g.
  # security-hardening `emptyDir`s); use `extraVolumes` below for anything you add on top, so
  # overriding one doesn't require repeating the other. Defaults to a `/tmp` `emptyDir`, which is
  # what makes the default `securityContext.readOnlyRootFilesystem: true` usable - drop it only if
  # you also relax that. Helm replaces lists wholesale rather than merging them, so overriding this
  # key means restating the entries you want to keep.
  # @section -- Backend
  volumes:
    - name: "tmp"
      emptyDir: {}
  # - name: config-volume
  #   configMap:
  #     name: log-config
  #     items:
  #       - key: log_level
  #         path: log_level.conf
  # - name: cache-volume
  #   emptyDir:
  #     sizeLimit: 500Mi
  # - name: image-volume
  #   image:
  #     reference: quay.io/crio/artifact:v2
  #     pullPolicy: IfNotPresent
  # -- Additional volumes to add, concatenated with `volumes` above at render time (e.g. to
  # mount a cert or config from a values override without repeating the chart's own volumes).
  # @section -- Backend
  extraVolumes: []
  # - name: certs
  #   secret:
  #     secretName: my-tls-cert
  # -- List of volumeClaims to add, rendered as the StatefulSet's `volumeClaimTemplates`. Requires
  # `deploymentType: "StatefulSet"` - setting it on a Deployment or DaemonSet fails at render time
  # rather than being silently dropped (use `volumes`/`extraVolumes` there instead).
  # @section -- Backend
  volumeClaims: []
  # - metadata:
  #     name: app-volume
  #   spec:
  #     accessModes: [ "ReadWriteOnce" ]
  #     storageClassName: "my-storage-class"
  #     resources:
  #       requests:
  #         storage: 1Gi
  # -- Additional volumeClaims to add, concatenated with `volumeClaims` above at render time.
  # @section -- Backend
  extraVolumeClaims: []
  # -- List of mounts to add (normally used with `volumes` or `volumeClaims`). Prefer this for
  # mounts the chart itself always needs; use `extraVolumeMounts` below for anything you add
  # on top, so overriding one doesn't require repeating the other. Defaults to the `/tmp` mount
  # backing the hardened `readOnlyRootFilesystem` default (see `volumes` above).
  # @section -- Backend
  volumeMounts:
    - name: "tmp"
      mountPath: "/tmp"
  # - name: config-volume
  #   mountPath: /app/config
  #   subPath: config
  #   readOnly: true
  #   mountPropagation: None
  #   recursiveReadOnly: Enabled
  # - name: cache-volume
  #   mountPath: /app/cache
  # - name: image-volume
  #   mountPath: /app/content
  # - name: storage-volume
  #   mountPath: /app/storage
  # -- Additional volumeMounts to add, concatenated with `volumeMounts` above at render time.
  # @section -- Backend
  extraVolumeMounts: []
  # - name: certs
  #   mountPath: /etc/certs
  #   readOnly: true
  service:
    # -- Whether or not to create a Service for the app. Set to `false` for components that
    # don't accept traffic (e.g. a queue consumer with no `containerPort`).
    # @section -- Backend
    enabled: true
    # -- Type of service to create for the app.
    # @section -- Backend
    type: "ClusterIP"
    # -- Port used by the service.
    # @section -- Backend
    port: 80
    # -- (int) Port used when type is `NodePort` to expose the service on the given node port. Left
    # empty, Kubernetes allocates one from the configured node-port range, which avoids two
    # releases of this chart colliding on the same hardcoded port.
    # @section -- Backend
    # @default -- `null` (allocated by Kubernetes)
    nodePort: null
    # -- Port name used by the service.
    # @section -- Backend
    portName: "http"
    # -- Protocol used by the service.
    # @section -- Backend
    protocol: "TCP"
    # -- Extra service ports.
    # @section -- Backend
    extraPorts: []
    # - port: 9090
    #   targetPort: 9090
    #   protocol: "TCP"
    #   name: "metrics"
  resources:
    requests:
      # -- Memory request for the app.
      # @section -- Backend
      memory: "256Mi"
      # -- CPU request for the app.
      # @section -- Backend
      cpu: "100m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "100Mi"
    limits:
      # -- Memory limit for the app.
      # @section -- Backend
      memory: "2Gi"
      # -- CPU limit for the app.
      # @section -- Backend
      cpu: "500m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "500Mi"
  autoscaling:
    # -- Enable Horizontal Pod Autoscaler for the app.
    # @section -- Backend
    enabled: false
    # -- Minimum number of replicas for the app.
    # @section -- Backend
    minReplicas: 1
    # -- Maximum number of replicas for the app.
    # @section -- Backend
    maxReplicas: 3
    # -- Average CPU utilization percentage for the app.
    # @section -- Backend
    targetCPUUtilizationPercentage: 80
    # -- Average memory utilization percentage for the app.
    # @section -- Backend
    targetMemoryUtilizationPercentage: 80
  # -- Revision history limit for the app.
  # @section -- Backend
  revisionHistoryLimit: 10
  # -- Default node selector for app.
  # @section -- Backend
  nodeSelector: {}
  # kubernetes.io/os: "linux"
  # kubernetes.io/arch: "amd64"
  # kubernetes.io/hostname: "node1"
  # -- Default tolerations for app.
  # @section -- Backend
  tolerations: []
  # - key: "key1"
  #   operator: "Equal"
  #   value: "value1"
  #   effect: "NoSchedule"
  #   tolerationSeconds: 3600
  # - key: "key2"
  #   operator: "Equal"
  #   value: "value2"
  #   effect: "NoExecute"
  #   tolerationSeconds: 3600
  # - key: "key3"
  #   operator: "Exists"
  #   effect: "PreferNoSchedule"
  #   tolerationSeconds: 3600
  # -- Affinity used for app pod.
  # @section -- Backend
  affinity: {}
  # podAntiAffinity:
  #   requiredDuringSchedulingIgnoredDuringExecution:
  #   - labelSelector:
  #       matchExpressions:
  #       - key: "app.kubernetes.io/name"
  #         operator: "In"
  #         values:
  #         - "digdigdoc-backend"
  #     topologyKey: "kubernetes.io/hostname"
  serviceAccount:
    # -- Enable the service account.
    # @section -- Backend
    enabled: false
    # -- Create a service account.
    # @section -- Backend
    create: false
    # -- Annotations applied to created service account.
    # @section -- Backend
    annotations: {}
    # -- Service account name.
    # @section -- Backend
    name: ""
    # -- Should the service account access token be automount in the pod.
    # @section -- Backend
    automountServiceAccountToken: false
    role:
      # -- Should the role be created.
      # @section -- Backend
      create: false
      # -- Role rules associated with the service account.
      # @section -- Backend
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
    clusterRole:
      # -- Should the clusterRole be created.
      # @section -- Backend
      create: false
      # -- ClusterRole rules associated with the service account.
      # @section -- Backend
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
  ## Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Deploy a PodDisruptionBudget for the app
    # @section -- Backend
    enabled: false
    # -- Labels to be added to app pdb.
    # @section -- Backend
    labels: {}
    # -- Annotations to be added to app pdb.
    # @section -- Backend
    annotations: {}
    # -- Number of pods that are available after eviction as number or percentage (eg.: 50%). One
    # of `minAvailable` / `maxUnavailable` must be set when `pdb.enabled` is true - a budget of 0
    # is the same as having no budget at all, so leaving both empty fails at render time.
    # @section -- Backend
    minAvailable: ""
    # -- Number of pods that are unavailable after eviction as number or percentage (eg.: 50%). Has higher precedence over `backend.pdb.minAvailable`.
    # @section -- Backend
    maxUnavailable: ""
  networkPolicy:
    # -- Create NetworkPolicy object for the app. The policy always selects this component's pods
    # only (via its selector labels), never the whole namespace.
    # @section -- Backend
    create: false
    # -- Labels to be added to the app NetworkPolicy.
    # @section -- Backend
    labels: {}
    # -- Annotations to be added to the app NetworkPolicy.
    # @section -- Backend
    annotations: {}
    # -- Policy types used in the NetworkPolicy object.
    # @section -- Backend
    policyTypes:
      - Ingress
    # -- Ingress rules for the NetworkPolicy object.
    # @section -- Backend
    ingress: []
    # - from:
    #   - ipBlock:
    #       cidr: 172.17.0.0/16
    #       except:
    #       - 172.17.1.0/24
    #   - namespaceSelector:
    #       matchLabels:
    #         project: myproject
    #   - podSelector:
    #       matchLabels:
    #         role: frontend
    #   ports:
    #   - protocol: TCP
    #     port: 6379
    # -- Egress rules for the NetworkPolicy object.
    # @section -- Backend
    egress: []
    # - to:
    #   - ipBlock:
    #       cidr: 10.0.0.0/24
    #   ports:
    #   - protocol: TCP
    #     port: 32000
    #     endPort: 32768
  metrics:
    # -- Deploy metrics service.
    # @section -- Backend
    enabled: false
    service:
      # -- Type of metrics service to create.
      # @section -- Backend
      type: "ClusterIP"
      # -- Metrics service annotations.
      # @section -- Backend
      annotations: {}
      # -- Metrics service labels.
      # @section -- Backend
      labels: {}
      # -- Metrics service port name.
      # @section -- Backend
      portName: "metrics"
      # -- Metrics service port.
      # @section -- Backend
      port: 9000
      # -- Metrics service target port.
      # @section -- Backend
      targetPort: 9000
    serviceMonitor:
      # -- Enable a prometheus ServiceMonitor.
      # @section -- Backend
      enabled: false
      # -- Prometheus ServiceMonitor labels.
      # @section -- Backend
      labels: {}
      # -- Prometheus ServiceMonitor annotations.
      # @section -- Backend
      annotations: {}
      endpoints:
        - basicAuth:
            # -- The secret in the service monitor namespace that contains the username for authentication.
            # @section -- Backend
            username: ""
            # -- The secret in the service monitor namespace that contains the password for authentication.
            # @section -- Backend
            password: ""
          bearerTokenSecret:
            # -- Secret name to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Backend
            name: ""
            # -- Secret key to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Backend
            key: ""
          # -- Prometheus ServiceMonitor interval.
          # @section -- Backend
          interval: "30s"
          # -- Prometheus ServiceMonitor scrapeTimeout. If empty, Prometheus uses the global scrape timeout unless it is less than the target's scrape interval value in which the latter is used.
          # @section -- Backend
          scrapeTimeout: "10s"
          # -- Path used by the Prometheus ServiceMonitor to scrape metrics.
          # @section -- Backend
          path: "/metrics"
          # -- When true, honorLabels preserves the metric’s labels when they collide with the target’s labels.
          # @section -- Backend
          honorLabels: false
          # -- Prometheus RelabelConfigs to apply to samples before scraping.
          # @section -- Backend
          relabelings: []
          # -- Prometheus MetricRelabelConfigs to apply to samples before ingestion.
          # @section -- Backend
          metricRelabelings: []
          # -- Prometheus ServiceMonitor selector.
          # @section -- Backend
          selector: {}
          # prometheus: kube-prometheus
          # -- Prometheus ServiceMonitor scheme.
          # @section -- Backend
          scheme: ""
          # -- Prometheus ServiceMonitor tlsConfig.
          # @section -- Backend
          tlsConfig: {}
worker_agent:
  # -- Workload kind to deploy the app as. One of "Deployment", "StatefulSet" or "DaemonSet"
  # (validated at render time - an unknown value fails instead of producing a release with no
  # workload). Use the top-level `jobs` / `cronjobs` maps for one-off or scheduled workloads.
  # Some values only apply to certain kinds: `replicaCount`/`autoscaling` and `strategy` are
  # Deployment-only (`autoscaling` also works on a StatefulSet), `volumeClaims`/`extraVolumeClaims`
  # are StatefulSet-only, and `updateStrategy` covers StatefulSet and DaemonSet.
  # @section -- Worker_agent
  deploymentType: "Deployment"
  # -- The number of application controller pods to run. Ignored when `deploymentType` is
  # "DaemonSet" (one pod per node) or when `autoscaling.enabled` is true.
  # @section -- Worker_agent
  replicaCount: 1
  image:
    # -- Registry to use for the app.
    # @section -- Worker_agent
    registry: "ghcr.io"
    # -- Repository to use for the app.
    # @section -- Worker_agent
    repository: "ia-generative/dig-dig-doc/worker-agent-execution"
    # -- Image pull policy for the app.
    # @section -- Worker_agent
    pullPolicy: "IfNotPresent"
    # -- Tag to use for the app.
    # @section -- Worker_agent
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
    # -- Image digest (`sha256:...`). When set it takes precedence over `tag`, pinning the exact
    # image content so the same release can never resolve to a different build - preferred over a
    # mutable tag for anything you deploy to production.
    # @section -- Worker_agent
    digest: ""
  ingress:
    # -- Whether or not ingress should be enabled.
    # @section -- Worker_agent
    enabled: false
    # -- Defines which ingress controller will implement the resource.
    # @section -- Worker_agent
    className: ""
    # -- Additional ingress annotations.
    # @section -- Worker_agent
    annotations: {}
    # -- Additional ingress labels.
    # @section -- Worker_agent
    labels: {}
    hosts:
      - # -- Name of the host record.
        # @section -- Worker_agent
        name: "domain.local"
        # -- Paths of the host record to manage routing (avoids repeating the same host for
        # multiple paths/backends).
        # @section -- Worker_agent
        paths:
          - # -- Path of the host record to manage routing.
            # @section -- Worker_agent
            path: "/"
            # -- Path type of the host record.
            # @section -- Worker_agent
            pathType: "Prefix"
            backend:
              # -- Name of the backend service linked to the path (leave empty to use the app service).
              # @section -- Worker_agent
              serviceName: ""
              # -- Port used by the backend service linked to the path (leave null to use the app service port).
              # @section -- Worker_agent
              portNumber: null
    # -- Enable TLS configuration.
    # @section -- Worker_agent
    tls: []
    # - secretName: domain.local-tls
    #   hosts:
    #   - domain.local
  ## Gateway API HTTPRoute configuration (alternative to Ingress)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/httproute/
  httpRoute:
    # -- Enable an HTTPRoute resource for this service.
    # @section -- Worker_agent
    enabled: false
    # -- Additional HTTPRoute annotations.
    # @section -- Worker_agent
    annotations: {}
    # -- Additional HTTPRoute labels.
    # @section -- Worker_agent
    labels: {}
    # -- Parent references (Gateways) to attach the HTTPRoute to.
    # @section -- Worker_agent
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace  # optional
    #   sectionName: https            # optional, specific listener name
    # -- Hostnames for the HTTPRoute to match.
    # @section -- Worker_agent
    hostnames: []
    # - "example.com"
    # - "api.example.com"
    # -- Routing rules for the HTTPRoute.
    # @section -- Worker_agent
    rules: []
    # - matches:
    #   - path:
    #       type: PathPrefix     # PathPrefix, Exact, or RegularExpression
    #       value: "/"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #     method: GET
    #   filters:
    #   - type: RequestHeaderModifier
    #     requestHeaderModifier:
    #       add:
    #       - name: "x-custom-header"
    #         value: "my-value"
    #   backendRefs:
    #   - name: my-service       # defaults to chart service if not set
    #     port: 80               # defaults to service port if not set
    #     weight: 100            # for traffic splitting
  ## Gateway API GRPCRoute configuration (for gRPC services)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/grpcroute/
  grpcRoute:
    # -- Enable a GRPCRoute resource for this service.
    # @section -- Worker_agent
    enabled: false
    # -- Additional GRPCRoute annotations.
    # @section -- Worker_agent
    annotations: {}
    # -- Additional GRPCRoute labels.
    # @section -- Worker_agent
    labels: {}
    # -- Parent references (Gateways) to attach the GRPCRoute to.
    # @section -- Worker_agent
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace
    #   sectionName: grpc
    # -- Hostnames for the GRPCRoute to match.
    # @section -- Worker_agent
    hostnames: []
    # - "grpc.example.com"
    # -- Routing rules for the GRPCRoute.
    # @section -- Worker_agent
    rules: []
    # - matches:
    #   - method:
    #       type: Exact          # Exact or RegularExpression
    #       service: "mypackage.MyService"
    #       method: "MyMethod"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #   backendRefs:
    #   - name: my-grpc-service
    #     port: 50051
    #     weight: 100
  # -- Mount the ServiceAccount token into the app pods. Defaults to false so a compromised
  # container holds no API credentials; the API server does not need the token unless the app
  # actually talks to the Kubernetes API. Applied at pod level so it holds even when
  # `serviceAccount.name` points at an SA that automounts.
  # @section -- Worker_agent
  automountServiceAccountToken: false
  # -- Inject the legacy `{SVC}_SERVICE_HOST`/`_PORT` environment variables for every Service in the
  # namespace. Defaults to false: the variables are rarely used, leak the namespace's topology into
  # every container, and can collide with the app's own configuration. Set to true only for an app
  # that genuinely reads them.
  # @section -- Worker_agent
  enableServiceLinks: false
  # -- Annotations for the app deployed pods.
  # @section -- Worker_agent
  podAnnotations: {}
  # -- Labels for the app deployed pods.
  # @section -- Worker_agent
  podLabels: {}
  # -- Pod-level security context. Defaults to a hardened baseline that satisfies the `restricted`
  # Pod Security Standard. Rendered via `toYaml`, so any `PodSecurityContext` field is accepted.
  # Adjust the UID/GID to whatever your image actually ships with - `runAsNonRoot` makes the
  # kubelet refuse to start a container that would run as root, which is the intended failure mode
  # rather than something to switch off. Set to `null` to omit the block entirely.
  # @section -- Worker_agent
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    # Owns mounted volumes so a non-root container can still write to them.
    fsGroup: 1000
    # Only chown volume contents when the top-level dir doesn't already match, which avoids
    # re-chowning large volumes on every start.
    fsGroupChangePolicy: "OnRootMismatch"
    seccompProfile:
      type: "RuntimeDefault"
  # -- Init containers to add to the app pod.
  # @section -- Worker_agent
  initContainers: []
  # - name: wait-for-keycloak
  #   image: docker.io/curlimages/curl:latest
  #   command:
  #   - "/bin/sh"
  #   - "-c"
  #   args:
  #   - "while [ $(curl -sw '%{http_code}' http://webapp.svc.cluster.local -o /dev/null) -ne 200 ]; do sleep 5; echo 'Waiting for the webapp...'; done"
  #   volumeMounts:
  #   - mountPath: /custom-volume
  #     name: custom-volume
  # -- Extra containers to add to the app pod as sidecars.
  # @section -- Worker_agent
  extraContainers: []
  # - name: fluentd
  #   image: "fluentd"
  #   volumeMounts:
  #     - mountPath: /my-volume/config
  #       name: config
  # -- Worker_agent container port number. Set to `null`/`0` (and disable `service`/probes) for
  # components that don't listen on any port (e.g. a queue consumer).
  # @section -- Worker_agent
  containerPort: &worker_agentContainerPort 8080
  # -- Worker_agent container port name.
  # @section -- Worker_agent
  containerPortName: "http"
  # -- Worker_agent extra container ports.
  # @section -- Worker_agent
  extraPorts: []
  # - containerPort: 9090
  #   protocol: "TCP"
  # -- Worker_agent container command.
  # @section -- Worker_agent
  command: []
  # -- Worker_agent container command args.
  # @section -- Worker_agent
  args: []
  # -- Container-level security context. Defaults to a hardened baseline that satisfies the
  # `restricted` Pod Security Standard: no privilege escalation, no capabilities, immutable root
  # filesystem. Rendered via `toYaml`, so any `SecurityContext` field is accepted. Note
  # `readOnlyRootFilesystem` requires the app to write only to mounted volumes - the default
  # `volumes`/`volumeMounts` below provide an `emptyDir` on /tmp for that reason. Set to `null` to
  # omit the block entirely.
  # @section -- Worker_agent
  securityContext:
    allowPrivilegeEscalation: false
    privileged: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    capabilities:
      drop:
        - "ALL"
  # -- Worker_agent container env variables loaded from configmap or secret reference. List or map
  # (merged with `global.envFrom` above, global entries first); see `global.envFrom` for both forms.
  # @section -- Worker_agent
  envFrom: []
  # - configMapRef:
  #     name: my-config
  # - secretRef:
  #     name: my-secret
  # -- Map or array of environment variables to inject into the app container (`valueFrom` supported).
  # @section -- Worker_agent
  env: {}
  # A: "A"
  # B:
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # C:
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  #
  # - name: "A"
  #   value: "A"
  # - name: "B"
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # - name: "C"
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  # -- Map of environment variables to inject into a configmap loaded by the app container (`valueFrom` not supported).
  # @section -- Worker_agent
  envCm: {}
  # D: "D"
  # -- Map of environment variables to inject into a secret loaded by the app container (`valueFrom`
  # not supported). Values placed here are stored in plain text in the values file AND in the Helm
  # release secret, so use it for non-sensitive-but-secret-shaped config only. For real credentials
  # prefer referencing a Secret you manage elsewhere via `envFrom`, or have an operator materialise
  # it (see the `VaultStaticSecret` example under `extraObjects`).
  # @section -- Worker_agent
  envSecret: {}
  # E: "E"
  ## Each probe is rendered verbatim via `toYaml`, so any `Probe` field (`exec`, `tcpSocket`,
  ## `grpc`...) is accepted, not just the `httpGet` shown here. To DISABLE a probe, set it to
  ## `null` - `{}` does not work, because Helm deep-merges values files and would merge these
  ## defaults straight back in.
  probes:
    startupProbe:
      httpGet:
        # -- Worker_agent container healthcheck endpoint (startupProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_agent
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_agent
        port: *worker_agentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_agent
      initialDelaySeconds: 0
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_agent
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_agent
      failureThreshold: 10
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_agent
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_agent
      timeoutSeconds: 5
    readinessProbe:
      httpGet:
        # -- Worker_agent container healthcheck endpoint (readinessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_agent
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_agent
        port: *worker_agentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_agent
      initialDelaySeconds: 10
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_agent
      successThreshold: 2
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_agent
      failureThreshold: 2
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_agent
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_agent
      timeoutSeconds: 5
    livenessProbe:
      httpGet:
        # -- Worker_agent container healthcheck endpoint (livenessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_agent
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_agent
        port: *worker_agentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_agent
      initialDelaySeconds: 30
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_agent
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_agent
      failureThreshold: 3
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_agent
      periodSeconds: 30
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_agent
      timeoutSeconds: 5
  ## Deployment-only. StatefulSet and DaemonSet use `updateStrategy` below - their update
  ## strategies are a different API shape (no `Recreate`, and different `rollingUpdate` fields),
  ## so they are kept as separate values rather than one block that means three different things.
  strategy:
    # -- Strategy type used to replace old Pods by new ones, can be `Recreate` or `RollingUpdate`.
    # Only applied when `deploymentType` is "Deployment".
    # @section -- Worker_agent
    type: "RollingUpdate"
    rollingUpdate:
      # -- The maximum number of pods that can be unavailable during the update process.
      # @section -- Worker_agent
      maxUnavailable: 1
      # -- The maximum number of pods that can be scheduled above the desired number of pods.
      # @section -- Worker_agent
      maxSurge: 1
  # -- Update strategy applied when `deploymentType` is "StatefulSet" or "DaemonSet" (ignored for
  # a Deployment, which uses `strategy` above). Rendered verbatim via `toYaml`, so it takes the
  # native `StatefulSetUpdateStrategy`/`DaemonSetUpdateStrategy` shape of the selected kind; left
  # empty, Kubernetes applies its own default (`RollingUpdate` for both).
  # @section -- Worker_agent
  updateStrategy: {}
  # StatefulSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     partition: 0
  #     maxUnavailable: 1
  # DaemonSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     maxUnavailable: 1
  #     maxSurge: 0
  # -- Image credentials configuration.
  # @section -- Worker_agent
  imagePullSecrets: []
  # - name: "pullsecret-name-1"
  #   create: false
  # - name: "pullsecret-name-2"
  #   create: true
  #   registry: ""
  #   username: ""
  #   password: ""
  #   email: ""
  ## Node-level pod settings. Mostly relevant when `deploymentType` is "DaemonSet" (node agents,
  ## log/metric collectors), but valid for every kind.
  # -- Share the host network namespace. Container ports then bind directly on the node, so they
  # must not collide with anything else running there.
  # @section -- Worker_agent
  hostNetwork: false
  # -- Share the host PID namespace (lets the container see and signal host processes).
  # @section -- Worker_agent
  hostPID: false
  # -- Pod DNS policy. Left empty, it defaults to `ClusterFirstWithHostNet` when `hostNetwork` is
  # true (otherwise a hostNetwork pod silently stops resolving cluster DNS) and to the Kubernetes
  # default `ClusterFirst` when it isn't.
  # @section -- Worker_agent
  # @default -- `""` (`ClusterFirstWithHostNet` when `hostNetwork` is true)
  dnsPolicy: ""
  # -- Pod DNS configuration, merged with `dnsPolicy` by the kubelet.
  # @section -- Worker_agent
  dnsConfig: {}
  # nameservers:
  # - "1.1.1.1"
  # options:
  # - name: "ndots"
  #   value: "2"
  # searches:
  # - "my.dns.search.suffix"
  # -- PriorityClass to schedule the pods with (e.g. `system-node-critical` for a node agent that
  # must not be evicted under pressure).
  # @section -- Worker_agent
  priorityClassName: ""
  # -- (int) Grace period, in seconds, given to the pod to shut down cleanly before it is killed.
  # @section -- Worker_agent
  # @default -- `null` (Kubernetes default of 30)
  terminationGracePeriodSeconds: null
  # -- Topology spread constraints used to spread the pods across failure domains.
  # @section -- Worker_agent
  topologySpreadConstraints: []
  # - maxSkew: 1
  #   topologyKey: "topology.kubernetes.io/zone"
  #   whenUnsatisfiable: "ScheduleAnyway"
  #   labelSelector:
  #     matchLabels:
  #       app.kubernetes.io/name: "digdigdoc-worker_agent"
  # -- Host aliases that will be injected at pod-level into /etc/hosts.
  # @section -- Worker_agent
  hostAliases: []
  # - ip: "127.0.0.1"
  #   hostnames:
  #   - "foo.local"
  #   - "bar.local"
  # - ip: "10.1.2.3"
  #   hostnames:
  #   - "foo.remote"
  #   - "bar.remote"
  # -- List of volumes to add. Prefer this for volumes the chart itself always needs (e.g.
  # security-hardening `emptyDir`s); use `extraVolumes` below for anything you add on top, so
  # overriding one doesn't require repeating the other. Defaults to a `/tmp` `emptyDir`, which is
  # what makes the default `securityContext.readOnlyRootFilesystem: true` usable - drop it only if
  # you also relax that. Helm replaces lists wholesale rather than merging them, so overriding this
  # key means restating the entries you want to keep.
  # @section -- Worker_agent
  volumes:
    - name: "tmp"
      emptyDir: {}
  # - name: config-volume
  #   configMap:
  #     name: log-config
  #     items:
  #       - key: log_level
  #         path: log_level.conf
  # - name: cache-volume
  #   emptyDir:
  #     sizeLimit: 500Mi
  # - name: image-volume
  #   image:
  #     reference: quay.io/crio/artifact:v2
  #     pullPolicy: IfNotPresent
  # -- Additional volumes to add, concatenated with `volumes` above at render time (e.g. to
  # mount a cert or config from a values override without repeating the chart's own volumes).
  # @section -- Worker_agent
  extraVolumes: []
  # - name: certs
  #   secret:
  #     secretName: my-tls-cert
  # -- List of volumeClaims to add, rendered as the StatefulSet's `volumeClaimTemplates`. Requires
  # `deploymentType: "StatefulSet"` - setting it on a Deployment or DaemonSet fails at render time
  # rather than being silently dropped (use `volumes`/`extraVolumes` there instead).
  # @section -- Worker_agent
  volumeClaims: []
  # - metadata:
  #     name: app-volume
  #   spec:
  #     accessModes: [ "ReadWriteOnce" ]
  #     storageClassName: "my-storage-class"
  #     resources:
  #       requests:
  #         storage: 1Gi
  # -- Additional volumeClaims to add, concatenated with `volumeClaims` above at render time.
  # @section -- Worker_agent
  extraVolumeClaims: []
  # -- List of mounts to add (normally used with `volumes` or `volumeClaims`). Prefer this for
  # mounts the chart itself always needs; use `extraVolumeMounts` below for anything you add
  # on top, so overriding one doesn't require repeating the other. Defaults to the `/tmp` mount
  # backing the hardened `readOnlyRootFilesystem` default (see `volumes` above).
  # @section -- Worker_agent
  volumeMounts:
    - name: "tmp"
      mountPath: "/tmp"
  # - name: config-volume
  #   mountPath: /app/config
  #   subPath: config
  #   readOnly: true
  #   mountPropagation: None
  #   recursiveReadOnly: Enabled
  # - name: cache-volume
  #   mountPath: /app/cache
  # - name: image-volume
  #   mountPath: /app/content
  # - name: storage-volume
  #   mountPath: /app/storage
  # -- Additional volumeMounts to add, concatenated with `volumeMounts` above at render time.
  # @section -- Worker_agent
  extraVolumeMounts: []
  # - name: certs
  #   mountPath: /etc/certs
  #   readOnly: true
  service:
    # -- Whether or not to create a Service for the app. Set to `false` for components that
    # don't accept traffic (e.g. a queue consumer with no `containerPort`).
    # @section -- Worker_agent
    enabled: true
    # -- Type of service to create for the app.
    # @section -- Worker_agent
    type: "ClusterIP"
    # -- Port used by the service.
    # @section -- Worker_agent
    port: 80
    # -- (int) Port used when type is `NodePort` to expose the service on the given node port. Left
    # empty, Kubernetes allocates one from the configured node-port range, which avoids two
    # releases of this chart colliding on the same hardcoded port.
    # @section -- Worker_agent
    # @default -- `null` (allocated by Kubernetes)
    nodePort: null
    # -- Port name used by the service.
    # @section -- Worker_agent
    portName: "http"
    # -- Protocol used by the service.
    # @section -- Worker_agent
    protocol: "TCP"
    # -- Extra service ports.
    # @section -- Worker_agent
    extraPorts: []
    # - port: 9090
    #   targetPort: 9090
    #   protocol: "TCP"
    #   name: "metrics"
  resources:
    requests:
      # -- Memory request for the app.
      # @section -- Worker_agent
      memory: "256Mi"
      # -- CPU request for the app.
      # @section -- Worker_agent
      cpu: "100m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "100Mi"
    limits:
      # -- Memory limit for the app.
      # @section -- Worker_agent
      memory: "2Gi"
      # -- CPU limit for the app.
      # @section -- Worker_agent
      cpu: "500m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "500Mi"
  autoscaling:
    # -- Enable Horizontal Pod Autoscaler for the app.
    # @section -- Worker_agent
    enabled: false
    # -- Minimum number of replicas for the app.
    # @section -- Worker_agent
    minReplicas: 1
    # -- Maximum number of replicas for the app.
    # @section -- Worker_agent
    maxReplicas: 3
    # -- Average CPU utilization percentage for the app.
    # @section -- Worker_agent
    targetCPUUtilizationPercentage: 80
    # -- Average memory utilization percentage for the app.
    # @section -- Worker_agent
    targetMemoryUtilizationPercentage: 80
  # -- Revision history limit for the app.
  # @section -- Worker_agent
  revisionHistoryLimit: 10
  # -- Default node selector for app.
  # @section -- Worker_agent
  nodeSelector: {}
  # kubernetes.io/os: "linux"
  # kubernetes.io/arch: "amd64"
  # kubernetes.io/hostname: "node1"
  # -- Default tolerations for app.
  # @section -- Worker_agent
  tolerations: []
  # - key: "key1"
  #   operator: "Equal"
  #   value: "value1"
  #   effect: "NoSchedule"
  #   tolerationSeconds: 3600
  # - key: "key2"
  #   operator: "Equal"
  #   value: "value2"
  #   effect: "NoExecute"
  #   tolerationSeconds: 3600
  # - key: "key3"
  #   operator: "Exists"
  #   effect: "PreferNoSchedule"
  #   tolerationSeconds: 3600
  # -- Affinity used for app pod.
  # @section -- Worker_agent
  affinity: {}
  # podAntiAffinity:
  #   requiredDuringSchedulingIgnoredDuringExecution:
  #   - labelSelector:
  #       matchExpressions:
  #       - key: "app.kubernetes.io/name"
  #         operator: "In"
  #         values:
  #         - "digdigdoc-worker_agent"
  #     topologyKey: "kubernetes.io/hostname"
  serviceAccount:
    # -- Enable the service account.
    # @section -- Worker_agent
    enabled: false
    # -- Create a service account.
    # @section -- Worker_agent
    create: false
    # -- Annotations applied to created service account.
    # @section -- Worker_agent
    annotations: {}
    # -- Service account name.
    # @section -- Worker_agent
    name: ""
    # -- Should the service account access token be automount in the pod.
    # @section -- Worker_agent
    automountServiceAccountToken: false
    role:
      # -- Should the role be created.
      # @section -- Worker_agent
      create: false
      # -- Role rules associated with the service account.
      # @section -- Worker_agent
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
    clusterRole:
      # -- Should the clusterRole be created.
      # @section -- Worker_agent
      create: false
      # -- ClusterRole rules associated with the service account.
      # @section -- Worker_agent
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
  ## Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Deploy a PodDisruptionBudget for the app
    # @section -- Worker_agent
    enabled: false
    # -- Labels to be added to app pdb.
    # @section -- Worker_agent
    labels: {}
    # -- Annotations to be added to app pdb.
    # @section -- Worker_agent
    annotations: {}
    # -- Number of pods that are available after eviction as number or percentage (eg.: 50%). One
    # of `minAvailable` / `maxUnavailable` must be set when `pdb.enabled` is true - a budget of 0
    # is the same as having no budget at all, so leaving both empty fails at render time.
    # @section -- Worker_agent
    minAvailable: ""
    # -- Number of pods that are unavailable after eviction as number or percentage (eg.: 50%). Has higher precedence over `worker_agent.pdb.minAvailable`.
    # @section -- Worker_agent
    maxUnavailable: ""
  networkPolicy:
    # -- Create NetworkPolicy object for the app. The policy always selects this component's pods
    # only (via its selector labels), never the whole namespace.
    # @section -- Worker_agent
    create: false
    # -- Labels to be added to the app NetworkPolicy.
    # @section -- Worker_agent
    labels: {}
    # -- Annotations to be added to the app NetworkPolicy.
    # @section -- Worker_agent
    annotations: {}
    # -- Policy types used in the NetworkPolicy object.
    # @section -- Worker_agent
    policyTypes:
      - Ingress
    # -- Ingress rules for the NetworkPolicy object.
    # @section -- Worker_agent
    ingress: []
    # - from:
    #   - ipBlock:
    #       cidr: 172.17.0.0/16
    #       except:
    #       - 172.17.1.0/24
    #   - namespaceSelector:
    #       matchLabels:
    #         project: myproject
    #   - podSelector:
    #       matchLabels:
    #         role: frontend
    #   ports:
    #   - protocol: TCP
    #     port: 6379
    # -- Egress rules for the NetworkPolicy object.
    # @section -- Worker_agent
    egress: []
    # - to:
    #   - ipBlock:
    #       cidr: 10.0.0.0/24
    #   ports:
    #   - protocol: TCP
    #     port: 32000
    #     endPort: 32768
  metrics:
    # -- Deploy metrics service.
    # @section -- Worker_agent
    enabled: false
    service:
      # -- Type of metrics service to create.
      # @section -- Worker_agent
      type: "ClusterIP"
      # -- Metrics service annotations.
      # @section -- Worker_agent
      annotations: {}
      # -- Metrics service labels.
      # @section -- Worker_agent
      labels: {}
      # -- Metrics service port name.
      # @section -- Worker_agent
      portName: "metrics"
      # -- Metrics service port.
      # @section -- Worker_agent
      port: 9000
      # -- Metrics service target port.
      # @section -- Worker_agent
      targetPort: 9000
    serviceMonitor:
      # -- Enable a prometheus ServiceMonitor.
      # @section -- Worker_agent
      enabled: false
      # -- Prometheus ServiceMonitor labels.
      # @section -- Worker_agent
      labels: {}
      # -- Prometheus ServiceMonitor annotations.
      # @section -- Worker_agent
      annotations: {}
      endpoints:
        - basicAuth:
            # -- The secret in the service monitor namespace that contains the username for authentication.
            # @section -- Worker_agent
            username: ""
            # -- The secret in the service monitor namespace that contains the password for authentication.
            # @section -- Worker_agent
            password: ""
          bearerTokenSecret:
            # -- Secret name to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Worker_agent
            name: ""
            # -- Secret key to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Worker_agent
            key: ""
          # -- Prometheus ServiceMonitor interval.
          # @section -- Worker_agent
          interval: "30s"
          # -- Prometheus ServiceMonitor scrapeTimeout. If empty, Prometheus uses the global scrape timeout unless it is less than the target's scrape interval value in which the latter is used.
          # @section -- Worker_agent
          scrapeTimeout: "10s"
          # -- Path used by the Prometheus ServiceMonitor to scrape metrics.
          # @section -- Worker_agent
          path: "/metrics"
          # -- When true, honorLabels preserves the metric’s labels when they collide with the target’s labels.
          # @section -- Worker_agent
          honorLabels: false
          # -- Prometheus RelabelConfigs to apply to samples before scraping.
          # @section -- Worker_agent
          relabelings: []
          # -- Prometheus MetricRelabelConfigs to apply to samples before ingestion.
          # @section -- Worker_agent
          metricRelabelings: []
          # -- Prometheus ServiceMonitor selector.
          # @section -- Worker_agent
          selector: {}
          # prometheus: kube-prometheus
          # -- Prometheus ServiceMonitor scheme.
          # @section -- Worker_agent
          scheme: ""
          # -- Prometheus ServiceMonitor tlsConfig.
          # @section -- Worker_agent
          tlsConfig: {}
worker_document:
  # -- Workload kind to deploy the app as. One of "Deployment", "StatefulSet" or "DaemonSet"
  # (validated at render time - an unknown value fails instead of producing a release with no
  # workload). Use the top-level `jobs` / `cronjobs` maps for one-off or scheduled workloads.
  # Some values only apply to certain kinds: `replicaCount`/`autoscaling` and `strategy` are
  # Deployment-only (`autoscaling` also works on a StatefulSet), `volumeClaims`/`extraVolumeClaims`
  # are StatefulSet-only, and `updateStrategy` covers StatefulSet and DaemonSet.
  # @section -- Worker_document
  deploymentType: "Deployment"
  # -- The number of application controller pods to run. Ignored when `deploymentType` is
  # "DaemonSet" (one pod per node) or when `autoscaling.enabled` is true.
  # @section -- Worker_document
  replicaCount: 1
  image:
    # -- Registry to use for the app.
    # @section -- Worker_document
    registry: "ghcr.io"
    # -- Repository to use for the app.
    # @section -- Worker_document
    repository: "ia-generative/dig-dig-doc/worker-document-process"
    # -- Image pull policy for the app.
    # @section -- Worker_document
    pullPolicy: "IfNotPresent"
    # -- Tag to use for the app.
    # @section -- Worker_document
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
    # -- Image digest (`sha256:...`). When set it takes precedence over `tag`, pinning the exact
    # image content so the same release can never resolve to a different build - preferred over a
    # mutable tag for anything you deploy to production.
    # @section -- Worker_document
    digest: ""
  ingress:
    # -- Whether or not ingress should be enabled.
    # @section -- Worker_document
    enabled: false
    # -- Defines which ingress controller will implement the resource.
    # @section -- Worker_document
    className: ""
    # -- Additional ingress annotations.
    # @section -- Worker_document
    annotations: {}
    # -- Additional ingress labels.
    # @section -- Worker_document
    labels: {}
    hosts:
      - # -- Name of the host record.
        # @section -- Worker_document
        name: "domain.local"
        # -- Paths of the host record to manage routing (avoids repeating the same host for
        # multiple paths/backends).
        # @section -- Worker_document
        paths:
          - # -- Path of the host record to manage routing.
            # @section -- Worker_document
            path: "/"
            # -- Path type of the host record.
            # @section -- Worker_document
            pathType: "Prefix"
            backend:
              # -- Name of the backend service linked to the path (leave empty to use the app service).
              # @section -- Worker_document
              serviceName: ""
              # -- Port used by the backend service linked to the path (leave null to use the app service port).
              # @section -- Worker_document
              portNumber: null
    # -- Enable TLS configuration.
    # @section -- Worker_document
    tls: []
    # - secretName: domain.local-tls
    #   hosts:
    #   - domain.local
  ## Gateway API HTTPRoute configuration (alternative to Ingress)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/httproute/
  httpRoute:
    # -- Enable an HTTPRoute resource for this service.
    # @section -- Worker_document
    enabled: false
    # -- Additional HTTPRoute annotations.
    # @section -- Worker_document
    annotations: {}
    # -- Additional HTTPRoute labels.
    # @section -- Worker_document
    labels: {}
    # -- Parent references (Gateways) to attach the HTTPRoute to.
    # @section -- Worker_document
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace  # optional
    #   sectionName: https            # optional, specific listener name
    # -- Hostnames for the HTTPRoute to match.
    # @section -- Worker_document
    hostnames: []
    # - "example.com"
    # - "api.example.com"
    # -- Routing rules for the HTTPRoute.
    # @section -- Worker_document
    rules: []
    # - matches:
    #   - path:
    #       type: PathPrefix     # PathPrefix, Exact, or RegularExpression
    #       value: "/"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #     method: GET
    #   filters:
    #   - type: RequestHeaderModifier
    #     requestHeaderModifier:
    #       add:
    #       - name: "x-custom-header"
    #         value: "my-value"
    #   backendRefs:
    #   - name: my-service       # defaults to chart service if not set
    #     port: 80               # defaults to service port if not set
    #     weight: 100            # for traffic splitting
  ## Gateway API GRPCRoute configuration (for gRPC services)
  ## Ref: https://gateway-api.sigs.k8s.io/api-types/grpcroute/
  grpcRoute:
    # -- Enable a GRPCRoute resource for this service.
    # @section -- Worker_document
    enabled: false
    # -- Additional GRPCRoute annotations.
    # @section -- Worker_document
    annotations: {}
    # -- Additional GRPCRoute labels.
    # @section -- Worker_document
    labels: {}
    # -- Parent references (Gateways) to attach the GRPCRoute to.
    # @section -- Worker_document
    parentRefs: []
    # - name: my-gateway
    #   namespace: gateway-namespace
    #   sectionName: grpc
    # -- Hostnames for the GRPCRoute to match.
    # @section -- Worker_document
    hostnames: []
    # - "grpc.example.com"
    # -- Routing rules for the GRPCRoute.
    # @section -- Worker_document
    rules: []
    # - matches:
    #   - method:
    #       type: Exact          # Exact or RegularExpression
    #       service: "mypackage.MyService"
    #       method: "MyMethod"
    #     headers:
    #     - type: Exact
    #       name: "x-version"
    #       value: "v1"
    #   backendRefs:
    #   - name: my-grpc-service
    #     port: 50051
    #     weight: 100
  # -- Mount the ServiceAccount token into the app pods. Defaults to false so a compromised
  # container holds no API credentials; the API server does not need the token unless the app
  # actually talks to the Kubernetes API. Applied at pod level so it holds even when
  # `serviceAccount.name` points at an SA that automounts.
  # @section -- Worker_document
  automountServiceAccountToken: false
  # -- Inject the legacy `{SVC}_SERVICE_HOST`/`_PORT` environment variables for every Service in the
  # namespace. Defaults to false: the variables are rarely used, leak the namespace's topology into
  # every container, and can collide with the app's own configuration. Set to true only for an app
  # that genuinely reads them.
  # @section -- Worker_document
  enableServiceLinks: false
  # -- Annotations for the app deployed pods.
  # @section -- Worker_document
  podAnnotations: {}
  # -- Labels for the app deployed pods.
  # @section -- Worker_document
  podLabels: {}
  # -- Pod-level security context. Defaults to a hardened baseline that satisfies the `restricted`
  # Pod Security Standard. Rendered via `toYaml`, so any `PodSecurityContext` field is accepted.
  # Adjust the UID/GID to whatever your image actually ships with - `runAsNonRoot` makes the
  # kubelet refuse to start a container that would run as root, which is the intended failure mode
  # rather than something to switch off. Set to `null` to omit the block entirely.
  # @section -- Worker_document
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    # Owns mounted volumes so a non-root container can still write to them.
    fsGroup: 1000
    # Only chown volume contents when the top-level dir doesn't already match, which avoids
    # re-chowning large volumes on every start.
    fsGroupChangePolicy: "OnRootMismatch"
    seccompProfile:
      type: "RuntimeDefault"
  # -- Init containers to add to the app pod.
  # @section -- Worker_document
  initContainers: []
  # - name: wait-for-keycloak
  #   image: docker.io/curlimages/curl:latest
  #   command:
  #   - "/bin/sh"
  #   - "-c"
  #   args:
  #   - "while [ $(curl -sw '%{http_code}' http://webapp.svc.cluster.local -o /dev/null) -ne 200 ]; do sleep 5; echo 'Waiting for the webapp...'; done"
  #   volumeMounts:
  #   - mountPath: /custom-volume
  #     name: custom-volume
  # -- Extra containers to add to the app pod as sidecars.
  # @section -- Worker_document
  extraContainers: []
  # - name: fluentd
  #   image: "fluentd"
  #   volumeMounts:
  #     - mountPath: /my-volume/config
  #       name: config
  # -- Worker_document container port number. Set to `null`/`0` (and disable `service`/probes) for
  # components that don't listen on any port (e.g. a queue consumer).
  # @section -- Worker_document
  containerPort: &worker_documentContainerPort 8080
  # -- Worker_document container port name.
  # @section -- Worker_document
  containerPortName: "http"
  # -- Worker_document extra container ports.
  # @section -- Worker_document
  extraPorts: []
  # - containerPort: 9090
  #   protocol: "TCP"
  # -- Worker_document container command.
  # @section -- Worker_document
  command: []
  # -- Worker_document container command args.
  # @section -- Worker_document
  args: []
  # -- Container-level security context. Defaults to a hardened baseline that satisfies the
  # `restricted` Pod Security Standard: no privilege escalation, no capabilities, immutable root
  # filesystem. Rendered via `toYaml`, so any `SecurityContext` field is accepted. Note
  # `readOnlyRootFilesystem` requires the app to write only to mounted volumes - the default
  # `volumes`/`volumeMounts` below provide an `emptyDir` on /tmp for that reason. Set to `null` to
  # omit the block entirely.
  # @section -- Worker_document
  securityContext:
    allowPrivilegeEscalation: false
    privileged: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    capabilities:
      drop:
        - "ALL"
  # -- Worker_document container env variables loaded from configmap or secret reference. List or map
  # (merged with `global.envFrom` above, global entries first); see `global.envFrom` for both forms.
  # @section -- Worker_document
  envFrom: []
  # - configMapRef:
  #     name: my-config
  # - secretRef:
  #     name: my-secret
  # -- Map or array of environment variables to inject into the app container (`valueFrom` supported).
  # @section -- Worker_document
  env: {}
  # A: "A"
  # B:
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # C:
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  #
  # - name: "A"
  #   value: "A"
  # - name: "B"
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "my-config"
  #       key: "B"
  # - name: "C"
  #   valueFrom:
  #     secretKeyRef:
  #       name: "my-secret"
  #       key: "C"
  # -- Map of environment variables to inject into a configmap loaded by the app container (`valueFrom` not supported).
  # @section -- Worker_document
  envCm: {}
  # D: "D"
  # -- Map of environment variables to inject into a secret loaded by the app container (`valueFrom`
  # not supported). Values placed here are stored in plain text in the values file AND in the Helm
  # release secret, so use it for non-sensitive-but-secret-shaped config only. For real credentials
  # prefer referencing a Secret you manage elsewhere via `envFrom`, or have an operator materialise
  # it (see the `VaultStaticSecret` example under `extraObjects`).
  # @section -- Worker_document
  envSecret: {}
  # E: "E"
  ## Each probe is rendered verbatim via `toYaml`, so any `Probe` field (`exec`, `tcpSocket`,
  ## `grpc`...) is accepted, not just the `httpGet` shown here. To DISABLE a probe, set it to
  ## `null` - `{}` does not work, because Helm deep-merges values files and would merge these
  ## defaults straight back in.
  probes:
    startupProbe:
      httpGet:
        # -- Worker_document container healthcheck endpoint (startupProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_document
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_document
        port: *worker_documentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_document
      initialDelaySeconds: 0
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_document
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_document
      failureThreshold: 10
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_document
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_document
      timeoutSeconds: 5
    readinessProbe:
      httpGet:
        # -- Worker_document container healthcheck endpoint (readinessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_document
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_document
        port: *worker_documentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_document
      initialDelaySeconds: 10
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_document
      successThreshold: 2
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_document
      failureThreshold: 2
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_document
      periodSeconds: 10
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_document
      timeoutSeconds: 5
    livenessProbe:
      httpGet:
        # -- Worker_document container healthcheck endpoint (livenessProbe is defined using `toYaml` so it is possible to override it completely).
        # @section -- Worker_document
        path: "/"
        # -- Port to use for healthcheck (defaults to container port).
        # @section -- Worker_document
        port: *worker_documentContainerPort
      # -- Number of seconds after the container has started before probe is initiated.
      # @section -- Worker_document
      initialDelaySeconds: 30
      # -- Minimum consecutive successes for the probe to be considered successful after having failed.
      # @section -- Worker_document
      successThreshold: 1
      # -- Minimum consecutive failures for the probe to be considered failed after having succeeded.
      # @section -- Worker_document
      failureThreshold: 3
      # -- How often (in seconds) to perform the probe.
      # @section -- Worker_document
      periodSeconds: 30
      # -- Number of seconds after which the probe times out.
      # @section -- Worker_document
      timeoutSeconds: 5
  ## Deployment-only. StatefulSet and DaemonSet use `updateStrategy` below - their update
  ## strategies are a different API shape (no `Recreate`, and different `rollingUpdate` fields),
  ## so they are kept as separate values rather than one block that means three different things.
  strategy:
    # -- Strategy type used to replace old Pods by new ones, can be `Recreate` or `RollingUpdate`.
    # Only applied when `deploymentType` is "Deployment".
    # @section -- Worker_document
    type: "RollingUpdate"
    rollingUpdate:
      # -- The maximum number of pods that can be unavailable during the update process.
      # @section -- Worker_document
      maxUnavailable: 1
      # -- The maximum number of pods that can be scheduled above the desired number of pods.
      # @section -- Worker_document
      maxSurge: 1
  # -- Update strategy applied when `deploymentType` is "StatefulSet" or "DaemonSet" (ignored for
  # a Deployment, which uses `strategy` above). Rendered verbatim via `toYaml`, so it takes the
  # native `StatefulSetUpdateStrategy`/`DaemonSetUpdateStrategy` shape of the selected kind; left
  # empty, Kubernetes applies its own default (`RollingUpdate` for both).
  # @section -- Worker_document
  updateStrategy: {}
  # StatefulSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     partition: 0
  #     maxUnavailable: 1
  # DaemonSet:
  #   type: "RollingUpdate"        # RollingUpdate or OnDelete
  #   rollingUpdate:
  #     maxUnavailable: 1
  #     maxSurge: 0
  # -- Image credentials configuration.
  # @section -- Worker_document
  imagePullSecrets: []
  # - name: "pullsecret-name-1"
  #   create: false
  # - name: "pullsecret-name-2"
  #   create: true
  #   registry: ""
  #   username: ""
  #   password: ""
  #   email: ""
  ## Node-level pod settings. Mostly relevant when `deploymentType` is "DaemonSet" (node agents,
  ## log/metric collectors), but valid for every kind.
  # -- Share the host network namespace. Container ports then bind directly on the node, so they
  # must not collide with anything else running there.
  # @section -- Worker_document
  hostNetwork: false
  # -- Share the host PID namespace (lets the container see and signal host processes).
  # @section -- Worker_document
  hostPID: false
  # -- Pod DNS policy. Left empty, it defaults to `ClusterFirstWithHostNet` when `hostNetwork` is
  # true (otherwise a hostNetwork pod silently stops resolving cluster DNS) and to the Kubernetes
  # default `ClusterFirst` when it isn't.
  # @section -- Worker_document
  # @default -- `""` (`ClusterFirstWithHostNet` when `hostNetwork` is true)
  dnsPolicy: ""
  # -- Pod DNS configuration, merged with `dnsPolicy` by the kubelet.
  # @section -- Worker_document
  dnsConfig: {}
  # nameservers:
  # - "1.1.1.1"
  # options:
  # - name: "ndots"
  #   value: "2"
  # searches:
  # - "my.dns.search.suffix"
  # -- PriorityClass to schedule the pods with (e.g. `system-node-critical` for a node agent that
  # must not be evicted under pressure).
  # @section -- Worker_document
  priorityClassName: ""
  # -- (int) Grace period, in seconds, given to the pod to shut down cleanly before it is killed.
  # @section -- Worker_document
  # @default -- `null` (Kubernetes default of 30)
  terminationGracePeriodSeconds: null
  # -- Topology spread constraints used to spread the pods across failure domains.
  # @section -- Worker_document
  topologySpreadConstraints: []
  # - maxSkew: 1
  #   topologyKey: "topology.kubernetes.io/zone"
  #   whenUnsatisfiable: "ScheduleAnyway"
  #   labelSelector:
  #     matchLabels:
  #       app.kubernetes.io/name: "digdigdoc-worker_document"
  # -- Host aliases that will be injected at pod-level into /etc/hosts.
  # @section -- Worker_document
  hostAliases: []
  # - ip: "127.0.0.1"
  #   hostnames:
  #   - "foo.local"
  #   - "bar.local"
  # - ip: "10.1.2.3"
  #   hostnames:
  #   - "foo.remote"
  #   - "bar.remote"
  # -- List of volumes to add. Prefer this for volumes the chart itself always needs (e.g.
  # security-hardening `emptyDir`s); use `extraVolumes` below for anything you add on top, so
  # overriding one doesn't require repeating the other. Defaults to a `/tmp` `emptyDir`, which is
  # what makes the default `securityContext.readOnlyRootFilesystem: true` usable - drop it only if
  # you also relax that. Helm replaces lists wholesale rather than merging them, so overriding this
  # key means restating the entries you want to keep.
  # @section -- Worker_document
  volumes:
    - name: "tmp"
      emptyDir: {}
  # - name: config-volume
  #   configMap:
  #     name: log-config
  #     items:
  #       - key: log_level
  #         path: log_level.conf
  # - name: cache-volume
  #   emptyDir:
  #     sizeLimit: 500Mi
  # - name: image-volume
  #   image:
  #     reference: quay.io/crio/artifact:v2
  #     pullPolicy: IfNotPresent
  # -- Additional volumes to add, concatenated with `volumes` above at render time (e.g. to
  # mount a cert or config from a values override without repeating the chart's own volumes).
  # @section -- Worker_document
  extraVolumes: []
  # - name: certs
  #   secret:
  #     secretName: my-tls-cert
  # -- List of volumeClaims to add, rendered as the StatefulSet's `volumeClaimTemplates`. Requires
  # `deploymentType: "StatefulSet"` - setting it on a Deployment or DaemonSet fails at render time
  # rather than being silently dropped (use `volumes`/`extraVolumes` there instead).
  # @section -- Worker_document
  volumeClaims: []
  # - metadata:
  #     name: app-volume
  #   spec:
  #     accessModes: [ "ReadWriteOnce" ]
  #     storageClassName: "my-storage-class"
  #     resources:
  #       requests:
  #         storage: 1Gi
  # -- Additional volumeClaims to add, concatenated with `volumeClaims` above at render time.
  # @section -- Worker_document
  extraVolumeClaims: []
  # -- List of mounts to add (normally used with `volumes` or `volumeClaims`). Prefer this for
  # mounts the chart itself always needs; use `extraVolumeMounts` below for anything you add
  # on top, so overriding one doesn't require repeating the other. Defaults to the `/tmp` mount
  # backing the hardened `readOnlyRootFilesystem` default (see `volumes` above).
  # @section -- Worker_document
  volumeMounts:
    - name: "tmp"
      mountPath: "/tmp"
  # - name: config-volume
  #   mountPath: /app/config
  #   subPath: config
  #   readOnly: true
  #   mountPropagation: None
  #   recursiveReadOnly: Enabled
  # - name: cache-volume
  #   mountPath: /app/cache
  # - name: image-volume
  #   mountPath: /app/content
  # - name: storage-volume
  #   mountPath: /app/storage
  # -- Additional volumeMounts to add, concatenated with `volumeMounts` above at render time.
  # @section -- Worker_document
  extraVolumeMounts: []
  # - name: certs
  #   mountPath: /etc/certs
  #   readOnly: true
  service:
    # -- Whether or not to create a Service for the app. Set to `false` for components that
    # don't accept traffic (e.g. a queue consumer with no `containerPort`).
    # @section -- Worker_document
    enabled: true
    # -- Type of service to create for the app.
    # @section -- Worker_document
    type: "ClusterIP"
    # -- Port used by the service.
    # @section -- Worker_document
    port: 80
    # -- (int) Port used when type is `NodePort` to expose the service on the given node port. Left
    # empty, Kubernetes allocates one from the configured node-port range, which avoids two
    # releases of this chart colliding on the same hardcoded port.
    # @section -- Worker_document
    # @default -- `null` (allocated by Kubernetes)
    nodePort: null
    # -- Port name used by the service.
    # @section -- Worker_document
    portName: "http"
    # -- Protocol used by the service.
    # @section -- Worker_document
    protocol: "TCP"
    # -- Extra service ports.
    # @section -- Worker_document
    extraPorts: []
    # - port: 9090
    #   targetPort: 9090
    #   protocol: "TCP"
    #   name: "metrics"
  resources:
    requests:
      # -- Memory request for the app.
      # @section -- Worker_document
      memory: "256Mi"
      # -- CPU request for the app.
      # @section -- Worker_document
      cpu: "100m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "100Mi"
    limits:
      # -- Memory limit for the app.
      # @section -- Worker_document
      memory: "2Gi"
      # -- CPU limit for the app.
      # @section -- Worker_document
      cpu: "500m"
      # Other stuff like GPU, etc could be added here.
      # nvidia.com/gpu: "1"
      # ephemeral-storage: "500Mi"
  autoscaling:
    # -- Enable Horizontal Pod Autoscaler for the app.
    # @section -- Worker_document
    enabled: false
    # -- Minimum number of replicas for the app.
    # @section -- Worker_document
    minReplicas: 1
    # -- Maximum number of replicas for the app.
    # @section -- Worker_document
    maxReplicas: 3
    # -- Average CPU utilization percentage for the app.
    # @section -- Worker_document
    targetCPUUtilizationPercentage: 80
    # -- Average memory utilization percentage for the app.
    # @section -- Worker_document
    targetMemoryUtilizationPercentage: 80
  # -- Revision history limit for the app.
  # @section -- Worker_document
  revisionHistoryLimit: 10
  # -- Default node selector for app.
  # @section -- Worker_document
  nodeSelector: {}
  # kubernetes.io/os: "linux"
  # kubernetes.io/arch: "amd64"
  # kubernetes.io/hostname: "node1"
  # -- Default tolerations for app.
  # @section -- Worker_document
  tolerations: []
  # - key: "key1"
  #   operator: "Equal"
  #   value: "value1"
  #   effect: "NoSchedule"
  #   tolerationSeconds: 3600
  # - key: "key2"
  #   operator: "Equal"
  #   value: "value2"
  #   effect: "NoExecute"
  #   tolerationSeconds: 3600
  # - key: "key3"
  #   operator: "Exists"
  #   effect: "PreferNoSchedule"
  #   tolerationSeconds: 3600
  # -- Affinity used for app pod.
  # @section -- Worker_document
  affinity: {}
  # podAntiAffinity:
  #   requiredDuringSchedulingIgnoredDuringExecution:
  #   - labelSelector:
  #       matchExpressions:
  #       - key: "app.kubernetes.io/name"
  #         operator: "In"
  #         values:
  #         - "digdigdoc-worker_document"
  #     topologyKey: "kubernetes.io/hostname"
  serviceAccount:
    # -- Enable the service account.
    # @section -- Worker_document
    enabled: false
    # -- Create a service account.
    # @section -- Worker_document
    create: false
    # -- Annotations applied to created service account.
    # @section -- Worker_document
    annotations: {}
    # -- Service account name.
    # @section -- Worker_document
    name: ""
    # -- Should the service account access token be automount in the pod.
    # @section -- Worker_document
    automountServiceAccountToken: false
    role:
      # -- Should the role be created.
      # @section -- Worker_document
      create: false
      # -- Role rules associated with the service account.
      # @section -- Worker_document
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
    clusterRole:
      # -- Should the clusterRole be created.
      # @section -- Worker_document
      create: false
      # -- ClusterRole rules associated with the service account.
      # @section -- Worker_document
      rules: []
      # - apiGroups:
      #   - argoproj.io
      #   resources:
      #   - applications
      #   - applicationsets
      #   - appprojects
      #   verbs:
      #   - create
      #   - get
      #   - list
      #   - watch
      #   - update
      #   - delete
      #   - patch
  ## Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Deploy a PodDisruptionBudget for the app
    # @section -- Worker_document
    enabled: false
    # -- Labels to be added to app pdb.
    # @section -- Worker_document
    labels: {}
    # -- Annotations to be added to app pdb.
    # @section -- Worker_document
    annotations: {}
    # -- Number of pods that are available after eviction as number or percentage (eg.: 50%). One
    # of `minAvailable` / `maxUnavailable` must be set when `pdb.enabled` is true - a budget of 0
    # is the same as having no budget at all, so leaving both empty fails at render time.
    # @section -- Worker_document
    minAvailable: ""
    # -- Number of pods that are unavailable after eviction as number or percentage (eg.: 50%). Has higher precedence over `worker_document.pdb.minAvailable`.
    # @section -- Worker_document
    maxUnavailable: ""
  networkPolicy:
    # -- Create NetworkPolicy object for the app. The policy always selects this component's pods
    # only (via its selector labels), never the whole namespace.
    # @section -- Worker_document
    create: false
    # -- Labels to be added to the app NetworkPolicy.
    # @section -- Worker_document
    labels: {}
    # -- Annotations to be added to the app NetworkPolicy.
    # @section -- Worker_document
    annotations: {}
    # -- Policy types used in the NetworkPolicy object.
    # @section -- Worker_document
    policyTypes:
      - Ingress
    # -- Ingress rules for the NetworkPolicy object.
    # @section -- Worker_document
    ingress: []
    # - from:
    #   - ipBlock:
    #       cidr: 172.17.0.0/16
    #       except:
    #       - 172.17.1.0/24
    #   - namespaceSelector:
    #       matchLabels:
    #         project: myproject
    #   - podSelector:
    #       matchLabels:
    #         role: frontend
    #   ports:
    #   - protocol: TCP
    #     port: 6379
    # -- Egress rules for the NetworkPolicy object.
    # @section -- Worker_document
    egress: []
    # - to:
    #   - ipBlock:
    #       cidr: 10.0.0.0/24
    #   ports:
    #   - protocol: TCP
    #     port: 32000
    #     endPort: 32768
  metrics:
    # -- Deploy metrics service.
    # @section -- Worker_document
    enabled: false
    service:
      # -- Type of metrics service to create.
      # @section -- Worker_document
      type: "ClusterIP"
      # -- Metrics service annotations.
      # @section -- Worker_document
      annotations: {}
      # -- Metrics service labels.
      # @section -- Worker_document
      labels: {}
      # -- Metrics service port name.
      # @section -- Worker_document
      portName: "metrics"
      # -- Metrics service port.
      # @section -- Worker_document
      port: 9000
      # -- Metrics service target port.
      # @section -- Worker_document
      targetPort: 9000
    serviceMonitor:
      # -- Enable a prometheus ServiceMonitor.
      # @section -- Worker_document
      enabled: false
      # -- Prometheus ServiceMonitor labels.
      # @section -- Worker_document
      labels: {}
      # -- Prometheus ServiceMonitor annotations.
      # @section -- Worker_document
      annotations: {}
      endpoints:
        - basicAuth:
            # -- The secret in the service monitor namespace that contains the username for authentication.
            # @section -- Worker_document
            username: ""
            # -- The secret in the service monitor namespace that contains the password for authentication.
            # @section -- Worker_document
            password: ""
          bearerTokenSecret:
            # -- Secret name to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Worker_document
            name: ""
            # -- Secret key to mount to read bearer token for scraping targets. The secret needs to be in the same namespace as the service monitor and accessible by the Prometheus Operator.
            # @section -- Worker_document
            key: ""
          # -- Prometheus ServiceMonitor interval.
          # @section -- Worker_document
          interval: "30s"
          # -- Prometheus ServiceMonitor scrapeTimeout. If empty, Prometheus uses the global scrape timeout unless it is less than the target's scrape interval value in which the latter is used.
          # @section -- Worker_document
          scrapeTimeout: "10s"
          # -- Path used by the Prometheus ServiceMonitor to scrape metrics.
          # @section -- Worker_document
          path: "/metrics"
          # -- When true, honorLabels preserves the metric’s labels when they collide with the target’s labels.
          # @section -- Worker_document
          honorLabels: false
          # -- Prometheus RelabelConfigs to apply to samples before scraping.
          # @section -- Worker_document
          relabelings: []
          # -- Prometheus MetricRelabelConfigs to apply to samples before ingestion.
          # @section -- Worker_document
          metricRelabelings: []
          # -- Prometheus ServiceMonitor selector.
          # @section -- Worker_document
          selector: {}
          # prometheus: kube-prometheus
          # -- Prometheus ServiceMonitor scheme.
          # @section -- Worker_document
          scheme: ""
          # -- Prometheus ServiceMonitor tlsConfig.
          # @section -- Worker_document
          tlsConfig: {}

## -- Redis subchart (cloudpirates/postgres chart uses the same pattern).
## Provides the Celery broker + result backend for the workers and the
## backend's SSE polling cache. The service is reachable at
## `digdigdoc-redis:6379` inside the cluster.
redis:
  enabled: true
  fullnameOverride: digdigdoc-redis
  auth:
    enabled: false
  architecture: standalone
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

## -- PostgreSQL subchart (cloudpirates/postgres).
## Provides the primary database for the backend. The service is reachable
## at `digdigdoc-postgres:5432` inside the cluster. The migration job and
## the backend both connect to this service.
postgres:
  enabled: true
  fullnameOverride: digdigdoc-postgres
  auth:
    username: digdigdoc
    password: digdigdoc
    database: digdigdoc
  primary:
    persistence:
      enabled: true
      size: 5Gi
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
