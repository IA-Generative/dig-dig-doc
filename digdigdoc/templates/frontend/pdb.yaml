{{- if .Values.enabled }}
{{- if .Values.frontend.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "frontend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "frontend")) | nindent 4 }}
  {{- if .Values.frontend.pdb.labels }}
    {{- toYaml .Values.frontend.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.frontend.pdb.annotations }}
  annotations: {{- toYaml .Values.frontend.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Tested against the empty string rather than for truthiness: `maxUnavailable: 0` is a valid
  setting (it blocks every voluntary eviction) but is falsy, so `with`/`if` would silently fall
  through to minAvailable. `toString` maps both nil and "" to "", which are the two unset forms.
  `helper.component.validate` guarantees at least one of the two is set. */ -}}
  {{- if ne (toString .Values.frontend.pdb.maxUnavailable) "" }}
  maxUnavailable: {{ .Values.frontend.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ .Values.frontend.pdb.minAvailable }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "frontend") | nindent 6 }}
{{- end }}
{{- end }}
