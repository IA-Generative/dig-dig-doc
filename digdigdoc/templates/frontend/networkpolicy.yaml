{{- if .Values.enabled }}
{{- if .Values.frontend.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "frontend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "frontend")) | nindent 4 }}
  {{- if .Values.frontend.networkPolicy.labels }}
    {{- toYaml .Values.frontend.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.frontend.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.frontend.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "frontend") | nindent 6 }}
  policyTypes: {{- toYaml .Values.frontend.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.frontend.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.frontend.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
