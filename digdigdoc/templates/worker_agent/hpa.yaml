{{- if .Values.enabled }}
{{- /* DaemonSet is excluded on purpose: it has no scale subresource, so an HPA targeting one is
rejected by the API server. `helper.component.validate` already fails on that combination - this
guard just keeps the template honest if the validation is ever relaxed. */ -}}
{{- if and .Values.worker_agent.autoscaling.enabled (ne .Values.worker_agent.deploymentType "DaemonSet") }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_agent")) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.worker_agent.deploymentType }}
    name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
  minReplicas: {{ .Values.worker_agent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.worker_agent.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.worker_agent.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_agent.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.worker_agent.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_agent.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
{{- end }}
