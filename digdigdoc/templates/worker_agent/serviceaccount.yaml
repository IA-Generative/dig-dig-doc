{{- if .Values.enabled }}
{{- if and .Values.worker_agent.serviceAccount.enabled .Values.worker_agent.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.worker_agent.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "worker_agent")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_agent")) | nindent 4 }}
  {{- if .Values.worker_agent.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker_agent.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.worker_agent.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
