{{- if .Values.enabled }}
{{- if .Values.worker_agent.ingress.enabled -}}
{{- $fullNameApp := include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") -}}
{{- $svcPortApp := .Values.worker_agent.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_agent")) | nindent 4 }}
  {{- if .Values.worker_agent.ingress.labels }}
    {{- toYaml .Values.worker_agent.ingress.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.worker_agent.ingress.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker_agent.ingress.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.worker_agent.ingress.className }}
  ingressClassName: {{ .Values.worker_agent.ingress.className }}
  {{- end }}
  {{- if gt (len .Values.worker_agent.ingress.tls) 0 }}
  tls:
  {{- range .Values.worker_agent.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . | quote }}
    {{- end }}
    secretName: {{ .secretName | default (printf "%s-%s" $fullNameApp "tls") | quote }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.worker_agent.ingress.hosts }}
  - host: {{ .name | quote }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
          {{- if .backend }}
            name: {{ .backend.serviceName | default $fullNameApp }}
            port:
              number: {{ .backend.portNumber | default $svcPortApp }}
          {{- else }}
            name: {{ $fullNameApp }}
            port:
              number: 80
          {{- end }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
