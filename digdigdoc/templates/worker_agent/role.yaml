{{- if .Values.enabled }}
{{- if and .Values.worker_agent.serviceAccount.enabled .Values.worker_agent.serviceAccount.role.create .Values.worker_agent.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_agent")) | nindent 4 }}
rules: {{- toYaml .Values.worker_agent.serviceAccount.role.rules | nindent 0 }}
{{- end }}
{{- end }}
