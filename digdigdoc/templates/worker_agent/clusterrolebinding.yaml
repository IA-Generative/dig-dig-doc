{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like clusterrole.yaml: without it, an empty `rules` list
yields a ClusterRoleBinding whose roleRef points at a ClusterRole that was never created. */ -}}
{{- if and .Values.worker_agent.serviceAccount.enabled .Values.worker_agent.serviceAccount.clusterRole.create .Values.worker_agent.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_agent")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_agent") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.worker_agent.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "worker_agent")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
