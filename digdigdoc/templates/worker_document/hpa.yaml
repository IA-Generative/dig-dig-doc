{{- if .Values.enabled }}
{{- /* DaemonSet is excluded on purpose: it has no scale subresource, so an HPA targeting one is
rejected by the API server. `helper.component.validate` already fails on that combination - this
guard just keeps the template honest if the validation is ever relaxed. */ -}}
{{- if and .Values.worker_document.autoscaling.enabled (ne .Values.worker_document.deploymentType "DaemonSet") }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.worker_document.deploymentType }}
    name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  minReplicas: {{ .Values.worker_document.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.worker_document.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.worker_document.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_document.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.worker_document.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_document.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
{{- end }}
