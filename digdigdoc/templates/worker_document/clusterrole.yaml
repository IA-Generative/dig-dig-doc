{{- if .Values.enabled }}
{{- if and .Values.worker_document.serviceAccount.enabled .Values.worker_document.serviceAccount.clusterRole.create .Values.worker_document.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
rules: {{- toYaml .Values.worker_document.serviceAccount.clusterRole.rules | nindent 0 }}
{{- end }}
{{- end }}
