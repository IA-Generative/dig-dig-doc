{{- if .Values.enabled }}
{{- if and .Values.worker_document.serviceAccount.enabled .Values.worker_document.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.worker_document.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "worker_document")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
  {{- if .Values.worker_document.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker_document.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.worker_document.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
