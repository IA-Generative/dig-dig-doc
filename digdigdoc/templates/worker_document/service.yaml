{{- if .Values.enabled }}
{{- /* Not gated on `deploymentType`: every supported kind (Deployment/StatefulSet/DaemonSet) can
sit behind a Service, and `service.enabled` is the switch for components that take no traffic.
This matches metrics.yaml, which is gated the same way. */ -}}
{{- if .Values.worker_document.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
spec:
  type: {{ .Values.worker_document.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.worker_document.service.port | default 80 }}
    targetPort: {{ .Values.worker_document.containerPortName | default .Values.worker_document.containerPort | default 8080 }}
    protocol: {{ .Values.worker_document.service.protocol | default "TCP" }}
    {{- if and (eq .Values.worker_document.service.type "NodePort") .Values.worker_document.service.nodePort }}
    nodePort: {{ .Values.worker_document.service.nodePort }}
    {{- end }}
    name: {{ .Values.worker_document.service.portName | default "http" }}
  {{- if .Values.worker_document.service.extraPorts -}}
  {{- toYaml .Values.worker_document.service.extraPorts | nindent 2 -}}
  {{- end }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "worker_document") | nindent 4 }}
{{- end }}
{{- end }}
