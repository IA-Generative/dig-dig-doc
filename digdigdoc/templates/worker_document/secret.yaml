{{- if .Values.enabled }}
{{- if or .Values.worker_document.envSecret .Values.global.envSecret -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
data:
  {{- if .Values.global.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.global.envSecret) | indent 2 }}
  {{- end -}}
  {{- if .Values.worker_document.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.worker_document.envSecret) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
