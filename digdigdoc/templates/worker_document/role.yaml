{{- if .Values.enabled }}
{{- if and .Values.worker_document.serviceAccount.enabled .Values.worker_document.serviceAccount.role.create .Values.worker_document.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_document") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_document")) | nindent 4 }}
rules: {{- toYaml .Values.worker_document.serviceAccount.role.rules | nindent 0 }}
{{- end }}
{{- end }}
