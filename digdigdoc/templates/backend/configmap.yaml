{{- if .Values.enabled }}
{{- if or .Values.backend.envCm .Values.global.envCm -}}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "backend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "backend")) | nindent 4 }}
data:
  {{- if .Values.global.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.global.envCm) | indent 2 }}
  {{- end -}}
  {{- if .Values.backend.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.backend.envCm) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
