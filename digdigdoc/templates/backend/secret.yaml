{{- if .Values.enabled }}
{{- if or .Values.backend.envSecret .Values.global.envSecret -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "backend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "backend")) | nindent 4 }}
data:
  {{- if .Values.global.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.global.envSecret) | indent 2 }}
  {{- end -}}
  {{- if .Values.backend.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.backend.envSecret) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
