{{- if .Values.enabled }}
{{- if and .Values.backend.serviceAccount.enabled .Values.backend.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.backend.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "backend")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "backend")) | nindent 4 }}
  {{- if .Values.backend.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.backend.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.backend.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
