{{- if .Values.enabled }}
{{- if .Values.backend.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "backend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "backend")) | nindent 4 }}
  {{- if .Values.backend.networkPolicy.labels }}
    {{- toYaml .Values.backend.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.backend.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.backend.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "backend") | nindent 6 }}
  policyTypes: {{- toYaml .Values.backend.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.backend.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.backend.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
