[project]
name = "dig-dig-doc-worker-agent-execution"
version = "0.3.0-rc.1"
description = "Worker Celery : exécution de l'agent d'instruction de dossier"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.3",
    "httpx>=0.28.1",
    "redis>=8.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.0",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
